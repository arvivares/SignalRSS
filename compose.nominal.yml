# Compatibility override for the nominal runtime.
#
# The base docker-compose.yml already defines the steady-state services. Legacy
# one-shot/category-specific workers are protected behind profiles, so this file
# intentionally avoids duplicating services with `extends`.
services: {}

volumes:
  postgres18_data:
