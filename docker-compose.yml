x-db-env: &db-env
  DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}

x-langfuse-env: &langfuse-env
  LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
  LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
  LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}

x-llm-env: &llm-env
  OPENAI_API_KEY: ${OPENAI_API_KEY:-}
  OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-${MATTERMOST_IMAGE_OPENROUTER_API_KEY:-}}
  OPENROUTER_REFERER: ${OPENROUTER_REFERER:-${MATTERMOST_IMAGE_OPENROUTER_REFERER:-}}
  OPENROUTER_TITLE: ${OPENROUTER_TITLE:-SignalRSS}
  OPENROUTER_REQUEST_TIMEOUT_MS: ${OPENROUTER_REQUEST_TIMEOUT_MS:-45000}
  GROQ_API_KEY: ${GROQ_API_KEY:-}
  GROQ_BASE_URL: ${GROQ_BASE_URL:-https://api.groq.com/openai/v1}
  GROQ_REQUEST_TIMEOUT_MS: ${GROQ_REQUEST_TIMEOUT_MS:-45000}
  CEREBRAS_API_KEY: ${CEREBRAS_API_KEY:-}
  CEREBRAS_BASE_URL: ${CEREBRAS_BASE_URL:-https://api.cerebras.ai/v1}
  CEREBRAS_REQUEST_TIMEOUT_MS: ${CEREBRAS_REQUEST_TIMEOUT_MS:-45000}
  SAMBANOVA_API_KEY: ${SAMBANOVA_API_KEY:-}
  SAMBANOVA_BASE_URL: ${SAMBANOVA_BASE_URL:-https://api.sambanova.ai/v1}
  SAMBANOVA_REQUEST_TIMEOUT_MS: ${SAMBANOVA_REQUEST_TIMEOUT_MS:-45000}
  MISTRAL_API_KEY: ${MISTRAL_API_KEY:-}
  MISTRAL_BASE_URL: ${MISTRAL_BASE_URL:-https://api.mistral.ai/v1}
  MISTRAL_REQUEST_TIMEOUT_MS: ${MISTRAL_REQUEST_TIMEOUT_MS:-45000}
  LLMRACK_API_KEY: ${LLMRACK_API_KEY:-}
  LLMRACK_BASE_URL: ${LLMRACK_BASE_URL:-https://llmrack.com/v1}
  LLMRACK_REQUEST_TIMEOUT_MS: ${LLMRACK_REQUEST_TIMEOUT_MS:-45000}
  CLOUDFLARE_API_TOKEN: ${CLOUDFLARE_API_TOKEN:-}
  CLOUDFLARE_ACCOUNT_ID: ${CLOUDFLARE_ACCOUNT_ID:-}
  CLOUDFLARE_BASE_URL: ${CLOUDFLARE_BASE_URL:-}
  CLOUDFLARE_REQUEST_TIMEOUT_MS: ${CLOUDFLARE_REQUEST_TIMEOUT_MS:-45000}
  GITHUB_MODELS_TOKEN: ${GITHUB_MODELS_TOKEN:-}
  GITHUB_MODELS_BASE_URL: ${GITHUB_MODELS_BASE_URL:-https://models.github.ai/inference}
  GITHUB_MODELS_REQUEST_TIMEOUT_MS: ${GITHUB_MODELS_REQUEST_TIMEOUT_MS:-45000}
  GEMINI_API_KEY: ${GEMINI_API_KEY:-}
  GEMINI_BASE_URL: ${GEMINI_BASE_URL:-https://generativelanguage.googleapis.com/v1beta/openai}
  GEMINI_NATIVE_BASE_URL: ${GEMINI_NATIVE_BASE_URL:-https://generativelanguage.googleapis.com/v1beta}
  GEMINI_REQUEST_TIMEOUT_MS: ${GEMINI_REQUEST_TIMEOUT_MS:-45000}
  GEMINI_25_THINKING_BUDGET: ${GEMINI_25_THINKING_BUDGET:-0}
  GEMINI_3_THINKING_LEVEL: ${GEMINI_3_THINKING_LEVEL:-LOW}
  NVIDIA_API_KEY: ${NVIDIA_API_KEY:-${MATTERMOST_IMAGE_NVIDIA_API_KEY:-}}
  NVIDIA_BASE_URL: ${NVIDIA_BASE_URL:-https://integrate.api.nvidia.com}
  NVIDIA_REQUEST_TIMEOUT_MS: ${NVIDIA_REQUEST_TIMEOUT_MS:-45000}
  LLM_COOLDOWN_ENABLED: ${LLM_COOLDOWN_ENABLED:-true}
  LLM_SAMBANOVA_FREE_RPM: ${LLM_SAMBANOVA_FREE_RPM:-20}
  LLM_GEMINI_RPM: ${LLM_GEMINI_RPM:-5}
  LLM_MISTRAL_RPM: ${LLM_MISTRAL_RPM:-6}
  LLM_LLMRACK_RPM: ${LLM_LLMRACK_RPM:-10}
  LLM_LLMRACK_TPD: ${LLM_LLMRACK_TPD:-10000}
  LLM_LLMRACK_IMPACT_MAX_BATCH_SIZE: ${LLM_LLMRACK_IMPACT_MAX_BATCH_SIZE:-2}
  LLM_LLMRACK_BRIEFING_MAX_BATCH_SIZE: ${LLM_LLMRACK_BRIEFING_MAX_BATCH_SIZE:-1}
  LLM_GITHUB_LOW_RPM: ${LLM_GITHUB_LOW_RPM:-15}
  LLM_GITHUB_IMPACT_MAX_BATCH_SIZE: ${LLM_GITHUB_IMPACT_MAX_BATCH_SIZE:-8}
  LLM_GITHUB_BRIEFING_MAX_BATCH_SIZE: ${LLM_GITHUB_BRIEFING_MAX_BATCH_SIZE:-2}
  LLM_OPENROUTER_IMPACT_MAX_BATCH_SIZE: ${LLM_OPENROUTER_IMPACT_MAX_BATCH_SIZE:-8}
  LLM_OPENROUTER_BRIEFING_MAX_BATCH_SIZE: ${LLM_OPENROUTER_BRIEFING_MAX_BATCH_SIZE:-2}
  LLM_OPENROUTER_FREE_RPM: ${LLM_OPENROUTER_FREE_RPM:-20}
  LLM_OPENROUTER_FREE_RPD: ${LLM_OPENROUTER_FREE_RPD:-50}
  LLM_MIN_COOLDOWN_MS: ${LLM_MIN_COOLDOWN_MS:-2000}
  LLM_MAX_COOLDOWN_MS: ${LLM_MAX_COOLDOWN_MS:-86400000}
  LLM_RATE_LIMIT_COOLDOWN_MS: ${LLM_RATE_LIMIT_COOLDOWN_MS:-60000}
  LLM_DAILY_RATE_LIMIT_COOLDOWN_MS: ${LLM_DAILY_RATE_LIMIT_COOLDOWN_MS:-86400000}
  LLM_PAYLOAD_TOO_LARGE_COOLDOWN_MS: ${LLM_PAYLOAD_TOO_LARGE_COOLDOWN_MS:-600000}
  LLM_TRANSPORT_ERROR_COOLDOWN_MS: ${LLM_TRANSPORT_ERROR_COOLDOWN_MS:-120000}
  LLM_BAD_RESPONSE_COOLDOWN_MS: ${LLM_BAD_RESPONSE_COOLDOWN_MS:-120000}
  LLM_MODEL_ERROR_COOLDOWN_MS: ${LLM_MODEL_ERROR_COOLDOWN_MS:-30000}

x-app-hardening: &app-hardening
  cap_drop:
    - ALL
  security_opt:
    - no-new-privileges:true

x-category-impact-worker-base: &category-impact-worker-base
  <<: *app-hardening
  build: .
  environment: &category-impact-worker-env
    <<: [*db-env, *llm-env, *langfuse-env]
    EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
    IMPACT_MODEL: ${IMPACT_MODEL:-gpt-5-nano}
    IMPACT_RUBRIC_VERSION: ${IMPACT_RUBRIC_VERSION:-impact-v2}
    IMPACT_CATEGORY_GUIDANCE_JSON: ${IMPACT_CATEGORY_GUIDANCE_JSON:-}
    IMPACT_MAX_OUTPUT_TOKENS: ${IMPACT_MAX_OUTPUT_TOKENS:-2200}
    IMPACT_MODEL_FALLBACKS: ${IMPACT_MODEL_FALLBACKS:-mistral:mistral-small-latest,nvidia:openai/gpt-oss-120b,github:openai/gpt-4.1-mini,github:openai/gpt-4o-mini,groq:openai/gpt-oss-120b,groq:llama-3.3-70b-versatile,groq:openai/gpt-oss-20b,openrouter:openai/gpt-oss-120b:free,openrouter:nvidia/nemotron-3-super-120b-a12b:free,sambanova:gpt-oss-120b,gemini:gemini-2.5-flash,cloudflare:@cf/openai/gpt-oss-120b,cerebras:llama3.1-8b,llmrack:qwen-2.5-7b,openai:gpt-5-nano}
    IMPACT_BATCH_SIZE: ${CATEGORY_IMPACT_BATCH_SIZE:-4}
    IMPACT_BATCHES_PER_TICK: ${CATEGORY_IMPACT_BATCHES_PER_TICK:-5}
    IMPACT_POLL_INTERVAL_SECONDS: ${CATEGORY_IMPACT_POLL_INTERVAL_SECONDS:-240}
    IMPACT_WINDOW_HOURS: ${IMPACT_WINDOW_HOURS:-168}
    IMPACT_MIN_PUBLISHED_AT: ${IMPACT_MIN_PUBLISHED_AT:-}
    IMPACT_JOB_STALE_MINUTES: ${IMPACT_JOB_STALE_MINUTES:-30}
    IMPACT_JOB_RETRY_MINUTES: ${IMPACT_JOB_RETRY_MINUTES:-10}
    IMPACT_JOB_MAX_ATTEMPTS: ${IMPACT_JOB_MAX_ATTEMPTS:-5}
    IMPACT_RUN_STALE_MINUTES: ${IMPACT_RUN_STALE_MINUTES:-30}
    CATEGORY_WORKER_INCLUDE_SLUGS: ${CATEGORY_WORKER_INCLUDE_SLUGS:-}
    CATEGORY_WORKER_EXCLUDE_SLUGS: ${CATEGORY_WORKER_EXCLUDE_SLUGS:-}
    CATEGORY_BRIEFING_EXCLUDE_LEVELS: ${CATEGORY_BRIEFING_EXCLUDE_LEVELS:-}
  command: ["npm", "run", "category-impact:worker"]
  depends_on:
    postgres:
      condition: service_healthy
    migrator:
      condition: service_completed_successfully
    category-seeder:
      condition: service_completed_successfully
  restart: unless-stopped

services:
  postgres:
    image: pgvector/pgvector:pg18
    command: ["postgres", "-c", "shared_preload_libraries=pg_stat_statements", "-c", "pg_stat_statements.track=all"]
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-signalrss}
      POSTGRES_USER: ${POSTGRES_USER:-signalrss}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-signalrss}
    ports:
      - "${POSTGRES_BIND_ADDRESS:-127.0.0.1}:5432:5432"
    volumes:
      - postgres18_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-signalrss} -d ${POSTGRES_DB:-signalrss}"]
      interval: 5s
      timeout: 5s
      retries: 10

  migrator:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
    command: ["npm", "run", "db:migrate"]
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  seeder:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
    command: ["npm", "run", "seed:feeds"]
    volumes:
      - ./data:/app/data:ro
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
    restart: "no"

  category-seeder:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
    command: ["npm", "run", "seed:categories"]
    volumes:
      - ./data:/app/data:ro
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
    restart: "no"

  backfill:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      INGEST_WINDOW_DAYS: ${INGEST_WINDOW_DAYS:-7}
      FEED_MAX_BYTES: ${FEED_MAX_BYTES:-10485760}
      BACKFILL_CONCURRENCY: ${BACKFILL_CONCURRENCY:-8}
    command: ["npm", "run", "backfill:7d"]
    profiles:
      - jobs
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      seeder:
        condition: service_completed_successfully
    restart: "no"

  classifier:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      CLASSIFIER_BATCH_SIZE: ${CLASSIFIER_BATCH_SIZE:-50}
      CLASSIFIER_POLL_INTERVAL_SECONDS: ${CLASSIFIER_POLL_INTERVAL_SECONDS:-300}
      CLASSIFIER_RUN_UNTIL_EMPTY: ${CLASSIFIER_RUN_UNTIL_EMPTY:-false}
      CLASSIFIER_MAX_SUMMARY_CHARS: ${CLASSIFIER_MAX_SUMMARY_CHARS:-1000}
      CLASSIFIER_MAX_CONTENT_CHARS: ${CLASSIFIER_MAX_CONTENT_CHARS:-500}
      CLASSIFIER_MAX_INPUT_CHARS: ${CLASSIFIER_MAX_INPUT_CHARS:-3000}
      CLASSIFIER_TOP_CATEGORIES: ${CLASSIFIER_TOP_CATEGORIES:-3}
      CLASSIFIER_MIN_CONFIDENCE: ${CLASSIFIER_MIN_CONFIDENCE:-0.18}
      CLASSIFIER_AI_MIN_CONFIDENCE: ${CLASSIFIER_AI_MIN_CONFIDENCE:-0.30}
      CLASSIFIER_MIN_MARGIN: ${CLASSIFIER_MIN_MARGIN:-0}
      CLASSIFIER_MIN_CONFIDENCE_CLOUD_INFRASTRUCTURE: ${CLASSIFIER_MIN_CONFIDENCE_CLOUD_INFRASTRUCTURE:-0.26}
      CLASSIFIER_MIN_MARGIN_CLOUD_INFRASTRUCTURE: ${CLASSIFIER_MIN_MARGIN_CLOUD_INFRASTRUCTURE:-0.02}
      CLASSIFIER_MIN_CONFIDENCE_AUTOMOTIVE_MOBILITY: ${CLASSIFIER_MIN_CONFIDENCE_AUTOMOTIVE_MOBILITY:-0.29}
      CLASSIFIER_MIN_MARGIN_AUTOMOTIVE_MOBILITY: ${CLASSIFIER_MIN_MARGIN_AUTOMOTIVE_MOBILITY:-0.01}
      CLASSIFIER_MIN_CONFIDENCE_GAMING: ${CLASSIFIER_MIN_CONFIDENCE_GAMING:-0.24}
      CLASSIFIER_MIN_MARGIN_GAMING: ${CLASSIFIER_MIN_MARGIN_GAMING:-0.01}
      CLASSIFIER_MIN_CONFIDENCE_CRYPTO_WEB3: ${CLASSIFIER_MIN_CONFIDENCE_CRYPTO_WEB3:-0.34}
      CLASSIFIER_MIN_MARGIN_CRYPTO_WEB3: ${CLASSIFIER_MIN_MARGIN_CRYPTO_WEB3:-0.02}
      CLASSIFIER_MIN_CONFIDENCE_SOFTWARE_DEVELOPMENT: ${CLASSIFIER_MIN_CONFIDENCE_SOFTWARE_DEVELOPMENT:-0.22}
      CLASSIFIER_MIN_MARGIN_SOFTWARE_DEVELOPMENT: ${CLASSIFIER_MIN_MARGIN_SOFTWARE_DEVELOPMENT:-0.01}
      CLASSIFIER_GUARD_AUTOMOTIVE_MOBILITY_ENABLED: ${CLASSIFIER_GUARD_AUTOMOTIVE_MOBILITY_ENABLED:-true}
      CLASSIFIER_GUARD_AUTOMOTIVE_STRICT_EXCLUDES: ${CLASSIFIER_GUARD_AUTOMOTIVE_STRICT_EXCLUDES:-true}
      CLASSIFIER_GUARD_STARTUPS_VENTURE_CAPITAL_ENABLED: ${CLASSIFIER_GUARD_STARTUPS_VENTURE_CAPITAL_ENABLED:-true}
      CLASSIFIER_GUARD_STARTUPS_VENTURE_CAPITAL_STRICT_EXCLUDES: ${CLASSIFIER_GUARD_STARTUPS_VENTURE_CAPITAL_STRICT_EXCLUDES:-true}
      CLASSIFIER_GUARD_SCIENCE_RESEARCH_ENABLED: ${CLASSIFIER_GUARD_SCIENCE_RESEARCH_ENABLED:-true}
      CLASSIFIER_GUARD_SCIENCE_RESEARCH_STRICT_EXCLUDES: ${CLASSIFIER_GUARD_SCIENCE_RESEARCH_STRICT_EXCLUDES:-true}
      CLASSIFIER_GUARD_POLICY_REGULATION_ENABLED: ${CLASSIFIER_GUARD_POLICY_REGULATION_ENABLED:-true}
      CLASSIFIER_GUARD_POLICY_REGULATION_STRICT_EXCLUDES: ${CLASSIFIER_GUARD_POLICY_REGULATION_STRICT_EXCLUDES:-true}
      CLASSIFIER_GUARD_CYBERSECURITY_ENABLED: ${CLASSIFIER_GUARD_CYBERSECURITY_ENABLED:-true}
      CLASSIFIER_GUARD_CYBERSECURITY_STRICT_EXCLUDES: ${CLASSIFIER_GUARD_CYBERSECURITY_STRICT_EXCLUDES:-true}
      CLASSIFIER_GUARD_GAMING_ENABLED: ${CLASSIFIER_GUARD_GAMING_ENABLED:-true}
      CLASSIFIER_GUARD_GAMING_STRICT_EXCLUDES: ${CLASSIFIER_GUARD_GAMING_STRICT_EXCLUDES:-true}
      CLASSIFIER_GUARD_FINTECH_CRYPTO_ENABLED: ${CLASSIFIER_GUARD_FINTECH_CRYPTO_ENABLED:-true}
      CLASSIFIER_GUARD_FINTECH_CRYPTO_STRICT_EXCLUDES: ${CLASSIFIER_GUARD_FINTECH_CRYPTO_STRICT_EXCLUDES:-true}
      CLASSIFIER_GUARD_CRYPTO_WEB3_ENABLED: ${CLASSIFIER_GUARD_CRYPTO_WEB3_ENABLED:-true}
      CLASSIFIER_GUARD_CRYPTO_WEB3_STRICT_EXCLUDES: ${CLASSIFIER_GUARD_CRYPTO_WEB3_STRICT_EXCLUDES:-true}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "classify:articles"]
    profiles:
      - jobs
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: "no"

  clusterer:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      CLUSTER_BATCH_SIZE: ${CLUSTER_BATCH_SIZE:-200}
      CLUSTER_POLL_INTERVAL_SECONDS: ${CLUSTER_POLL_INTERVAL_SECONDS:-300}
      CLUSTER_SIMILARITY_THRESHOLD: ${CLUSTER_SIMILARITY_THRESHOLD:-0.65}
      CLUSTER_AI_SIMILARITY_THRESHOLD: ${CLUSTER_AI_SIMILARITY_THRESHOLD:-0.78}
      CYBERSECURITY_CLUSTER_SIMILARITY_THRESHOLD: ${CYBERSECURITY_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CLOUD_INFRASTRUCTURE_CLUSTER_SIMILARITY_THRESHOLD: ${CLOUD_INFRASTRUCTURE_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      AUTOMOTIVE_MOBILITY_CLUSTER_SIMILARITY_THRESHOLD: ${AUTOMOTIVE_MOBILITY_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      SEMICONDUCTORS_CLUSTER_SIMILARITY_THRESHOLD: ${SEMICONDUCTORS_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      SOFTWARE_DEVELOPMENT_CLUSTER_SIMILARITY_THRESHOLD: ${SOFTWARE_DEVELOPMENT_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CONSUMER_ELECTRONICS_CLUSTER_SIMILARITY_THRESHOLD: ${CONSUMER_ELECTRONICS_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      GAMING_CLUSTER_SIMILARITY_THRESHOLD: ${GAMING_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      FINTECH_CRYPTO_CLUSTER_SIMILARITY_THRESHOLD: ${FINTECH_CRYPTO_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CRYPTO_WEB3_CLUSTER_SIMILARITY_THRESHOLD: ${CRYPTO_WEB3_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      ENTERPRISE_TECHNOLOGY_CLUSTER_SIMILARITY_THRESHOLD: ${ENTERPRISE_TECHNOLOGY_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      STARTUPS_VENTURE_CAPITAL_CLUSTER_SIMILARITY_THRESHOLD: ${STARTUPS_VENTURE_CAPITAL_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      SCIENCE_RESEARCH_CLUSTER_SIMILARITY_THRESHOLD: ${SCIENCE_RESEARCH_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      POLICY_REGULATION_CLUSTER_SIMILARITY_THRESHOLD: ${POLICY_REGULATION_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CLUSTER_CATEGORY_SLUG: ${CLUSTER_CATEGORY_SLUG:-}
      CLUSTER_WINDOW_DAYS: ${CLUSTER_WINDOW_DAYS:-7}
      CLUSTER_RUN_UNTIL_EMPTY: ${CLUSTER_RUN_UNTIL_EMPTY:-false}
    command: ["npm", "run", "cluster:articles"]
    profiles:
      - jobs
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: "no"

  cluster-merge:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      CLUSTER_MERGE_BATCH_SIZE: ${CLUSTER_MERGE_BATCH_SIZE:-50}
      CLUSTER_MERGE_POLL_INTERVAL_SECONDS: ${CLUSTER_MERGE_POLL_INTERVAL_SECONDS:-300}
      CLUSTER_MERGE_SIMILARITY_THRESHOLD: ${CLUSTER_MERGE_SIMILARITY_THRESHOLD:-0.82}
      CLUSTER_MERGE_CATEGORY_SLUG: ${CLUSTER_MERGE_CATEGORY_SLUG:-artificial-intelligence}
      CLUSTER_MERGE_WINDOW_DAYS: ${CLUSTER_MERGE_WINDOW_DAYS:-7}
      CLUSTER_MERGE_RUN_UNTIL_STABLE: ${CLUSTER_MERGE_RUN_UNTIL_STABLE:-false}
    command: ["npm", "run", "merge:clusters"]
    profiles:
      - jobs
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: "no"

  p0-cluster-adjudicator:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P0_ADJUDICATION_MODEL: ${P0_ADJUDICATION_MODEL:-gpt-5-nano}
      P0_ADJUDICATION_BATCH_SIZE: ${P0_ADJUDICATION_BATCH_SIZE:-12}
      P0_ADJUDICATION_POLL_INTERVAL_SECONDS: ${P0_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P0_ADJUDICATION_WINDOW_HOURS: ${P0_ADJUDICATION_WINDOW_HOURS:-168}
      P0_ADJUDICATION_CATEGORY_SLUG: ${P0_ADJUDICATION_CATEGORY_SLUG:-artificial-intelligence}
      P0_ADJUDICATION_MIN_CENTROID_SIMILARITY: ${P0_ADJUDICATION_MIN_CENTROID_SIMILARITY:-0.70}
      P0_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P0_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P0_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${P0_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.72}
      P0_ADJUDICATION_MERGE_CONFIDENCE: ${P0_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "adjudicate:p0-clusters"]
    profiles:
      - jobs
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: "no"

  p0-adjudication-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P0_ADJUDICATION_MODEL: ${P0_ADJUDICATION_MODEL:-gpt-5-nano}
      P0_ADJUDICATION_BATCH_SIZE: ${P0_ADJUDICATION_BATCH_SIZE:-12}
      P0_ADJUDICATION_POLL_INTERVAL_SECONDS: ${P0_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P0_ADJUDICATION_WINDOW_HOURS: ${P0_ADJUDICATION_WINDOW_HOURS:-168}
      P0_ADJUDICATION_CATEGORY_SLUG: ${P0_ADJUDICATION_CATEGORY_SLUG:-artificial-intelligence}
      P0_ADJUDICATION_MIN_CENTROID_SIMILARITY: ${P0_ADJUDICATION_MIN_CENTROID_SIMILARITY:-0.70}
      P0_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P0_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P0_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${P0_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.72}
      P0_ADJUDICATION_MERGE_CONFIDENCE: ${P0_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p0-adjudication:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  p1-cluster-adjudicator:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P1_ADJUDICATION_MODEL: ${P1_ADJUDICATION_MODEL:-gpt-5-nano}
      P1_ADJUDICATION_BATCH_SIZE: ${P1_ADJUDICATION_BATCH_SIZE:-20}
      P1_ADJUDICATION_POLL_INTERVAL_SECONDS: ${P1_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P1_ADJUDICATION_WINDOW_HOURS: ${P1_ADJUDICATION_WINDOW_HOURS:-168}
      P1_ADJUDICATION_CATEGORY_SLUG: ${P1_ADJUDICATION_CATEGORY_SLUG:-artificial-intelligence}
      P1_ADJUDICATION_P1P1_MIN_CENTROID_SIMILARITY: ${P1_ADJUDICATION_P1P1_MIN_CENTROID_SIMILARITY:-0.72}
      P1_ADJUDICATION_P1P0_MIN_CENTROID_SIMILARITY: ${P1_ADJUDICATION_P1P0_MIN_CENTROID_SIMILARITY:-0.70}
      P1_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P1_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P1_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${P1_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.74}
      P1_ADJUDICATION_MERGE_CONFIDENCE: ${P1_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "adjudicate:p1-clusters"]
    profiles:
      - jobs
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: "no"

  p1-adjudication-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P1_ADJUDICATION_MODEL: ${P1_ADJUDICATION_MODEL:-gpt-5-nano}
      P1_ADJUDICATION_BATCH_SIZE: ${P1_ADJUDICATION_BATCH_SIZE:-20}
      P1_ADJUDICATION_POLL_INTERVAL_SECONDS: ${P1_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P1_ADJUDICATION_WINDOW_HOURS: ${P1_ADJUDICATION_WINDOW_HOURS:-168}
      P1_ADJUDICATION_CATEGORY_SLUG: ${P1_ADJUDICATION_CATEGORY_SLUG:-artificial-intelligence}
      P1_ADJUDICATION_P1P1_MIN_CENTROID_SIMILARITY: ${P1_ADJUDICATION_P1P1_MIN_CENTROID_SIMILARITY:-0.72}
      P1_ADJUDICATION_P1P0_MIN_CENTROID_SIMILARITY: ${P1_ADJUDICATION_P1P0_MIN_CENTROID_SIMILARITY:-0.70}
      P1_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P1_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P1_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${P1_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.74}
      P1_ADJUDICATION_MERGE_CONFIDENCE: ${P1_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p1-adjudication:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  p2-cluster-adjudicator:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P2_ADJUDICATION_MODEL: ${P2_ADJUDICATION_MODEL:-gpt-5-nano}
      P2_ADJUDICATION_BATCH_SIZE: ${P2_ADJUDICATION_BATCH_SIZE:-20}
      P2_ADJUDICATION_POLL_INTERVAL_SECONDS: ${P2_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P2_ADJUDICATION_WINDOW_HOURS: ${P2_ADJUDICATION_WINDOW_HOURS:-168}
      P2_ADJUDICATION_CATEGORY_SLUG: ${P2_ADJUDICATION_CATEGORY_SLUG:-artificial-intelligence}
      P2_ADJUDICATION_P2P2_MIN_CENTROID_SIMILARITY: ${P2_ADJUDICATION_P2P2_MIN_CENTROID_SIMILARITY:-0.72}
      P2_ADJUDICATION_P2P1_MIN_CENTROID_SIMILARITY: ${P2_ADJUDICATION_P2P1_MIN_CENTROID_SIMILARITY:-0.70}
      P2_ADJUDICATION_P2P0_MIN_CENTROID_SIMILARITY: ${P2_ADJUDICATION_P2P0_MIN_CENTROID_SIMILARITY:-0.70}
      P2_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P2_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P2_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${P2_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.72}
      P2_ADJUDICATION_MERGE_CONFIDENCE: ${P2_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "adjudicate:p2-clusters"]
    profiles:
      - jobs
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: "no"

  p2-adjudication-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P2_ADJUDICATION_MODEL: ${P2_ADJUDICATION_MODEL:-gpt-5-nano}
      P2_ADJUDICATION_BATCH_SIZE: ${P2_ADJUDICATION_BATCH_SIZE:-20}
      P2_ADJUDICATION_POLL_INTERVAL_SECONDS: ${P2_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P2_ADJUDICATION_WINDOW_HOURS: ${P2_ADJUDICATION_WINDOW_HOURS:-168}
      P2_ADJUDICATION_CATEGORY_SLUG: ${P2_ADJUDICATION_CATEGORY_SLUG:-artificial-intelligence}
      P2_ADJUDICATION_P2P2_MIN_CENTROID_SIMILARITY: ${P2_ADJUDICATION_P2P2_MIN_CENTROID_SIMILARITY:-0.72}
      P2_ADJUDICATION_P2P1_MIN_CENTROID_SIMILARITY: ${P2_ADJUDICATION_P2P1_MIN_CENTROID_SIMILARITY:-0.70}
      P2_ADJUDICATION_P2P0_MIN_CENTROID_SIMILARITY: ${P2_ADJUDICATION_P2P0_MIN_CENTROID_SIMILARITY:-0.70}
      P2_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P2_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P2_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${P2_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.72}
      P2_ADJUDICATION_MERGE_CONFIDENCE: ${P2_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p2-adjudication:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  p3-cluster-adjudicator:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P3_ADJUDICATION_MODEL: ${P3_ADJUDICATION_MODEL:-gpt-5-nano}
      P3_ADJUDICATION_BATCH_SIZE: ${P3_ADJUDICATION_BATCH_SIZE:-20}
      P3_ADJUDICATION_POLL_INTERVAL_SECONDS: ${P3_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P3_ADJUDICATION_WINDOW_HOURS: ${P3_ADJUDICATION_WINDOW_HOURS:-168}
      P3_ADJUDICATION_CATEGORY_SLUG: ${P3_ADJUDICATION_CATEGORY_SLUG:-artificial-intelligence}
      P3_ADJUDICATION_P3P3_MIN_CENTROID_SIMILARITY: ${P3_ADJUDICATION_P3P3_MIN_CENTROID_SIMILARITY:-0.72}
      P3_ADJUDICATION_P3P2_MIN_CENTROID_SIMILARITY: ${P3_ADJUDICATION_P3P2_MIN_CENTROID_SIMILARITY:-0.70}
      P3_ADJUDICATION_P3P1_MIN_CENTROID_SIMILARITY: ${P3_ADJUDICATION_P3P1_MIN_CENTROID_SIMILARITY:-0.70}
      P3_ADJUDICATION_P3P0_MIN_CENTROID_SIMILARITY: ${P3_ADJUDICATION_P3P0_MIN_CENTROID_SIMILARITY:-0.70}
      P3_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P3_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P3_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${P3_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.72}
      P3_ADJUDICATION_MERGE_CONFIDENCE: ${P3_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "adjudicate:p3-clusters"]
    profiles:
      - jobs
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: "no"

  p3-adjudication-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P3_ADJUDICATION_MODEL: ${P3_ADJUDICATION_MODEL:-gpt-5-nano}
      P3_ADJUDICATION_BATCH_SIZE: ${P3_ADJUDICATION_BATCH_SIZE:-20}
      P3_ADJUDICATION_POLL_INTERVAL_SECONDS: ${P3_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P3_ADJUDICATION_WINDOW_HOURS: ${P3_ADJUDICATION_WINDOW_HOURS:-168}
      P3_ADJUDICATION_CATEGORY_SLUG: ${P3_ADJUDICATION_CATEGORY_SLUG:-artificial-intelligence}
      P3_ADJUDICATION_P3P3_MIN_CENTROID_SIMILARITY: ${P3_ADJUDICATION_P3P3_MIN_CENTROID_SIMILARITY:-0.72}
      P3_ADJUDICATION_P3P2_MIN_CENTROID_SIMILARITY: ${P3_ADJUDICATION_P3P2_MIN_CENTROID_SIMILARITY:-0.70}
      P3_ADJUDICATION_P3P1_MIN_CENTROID_SIMILARITY: ${P3_ADJUDICATION_P3P1_MIN_CENTROID_SIMILARITY:-0.70}
      P3_ADJUDICATION_P3P0_MIN_CENTROID_SIMILARITY: ${P3_ADJUDICATION_P3P0_MIN_CENTROID_SIMILARITY:-0.70}
      P3_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P3_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P3_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${P3_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.72}
      P3_ADJUDICATION_MERGE_CONFIDENCE: ${P3_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p3-adjudication:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  impact-scorer:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-${MATTERMOST_IMAGE_OPENROUTER_API_KEY:-}}
      OPENROUTER_REFERER: ${OPENROUTER_REFERER:-${MATTERMOST_IMAGE_OPENROUTER_REFERER:-}}
      OPENROUTER_TITLE: ${OPENROUTER_TITLE:-SignalRSS}
      OPENROUTER_REQUEST_TIMEOUT_MS: ${OPENROUTER_REQUEST_TIMEOUT_MS:-45000}
      GROQ_API_KEY: ${GROQ_API_KEY:-}
      GROQ_BASE_URL: ${GROQ_BASE_URL:-https://api.groq.com/openai/v1}
      GROQ_REQUEST_TIMEOUT_MS: ${GROQ_REQUEST_TIMEOUT_MS:-45000}
      NVIDIA_API_KEY: ${NVIDIA_API_KEY:-${MATTERMOST_IMAGE_NVIDIA_API_KEY:-}}
      NVIDIA_BASE_URL: ${NVIDIA_BASE_URL:-https://integrate.api.nvidia.com}
      NVIDIA_REQUEST_TIMEOUT_MS: ${NVIDIA_REQUEST_TIMEOUT_MS:-45000}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      IMPACT_MODEL: ${IMPACT_MODEL:-gpt-5-nano}
      IMPACT_RUBRIC_VERSION: ${IMPACT_RUBRIC_VERSION:-impact-v2}
      IMPACT_CATEGORY_GUIDANCE_JSON: ${IMPACT_CATEGORY_GUIDANCE_JSON:-}
      IMPACT_MAX_OUTPUT_TOKENS: ${IMPACT_MAX_OUTPUT_TOKENS:-2200}
      IMPACT_MODEL_FALLBACKS: ${IMPACT_MODEL_FALLBACKS:-nvidia:openai/gpt-oss-120b,groq:openai/gpt-oss-120b,openrouter:openai/gpt-oss-120b:free,groq:llama-3.3-70b-versatile,groq:openai/gpt-oss-20b,openrouter:nvidia/nemotron-3-super-120b-a12b:free,openai:gpt-5-nano}
      IMPACT_BATCH_SIZE: ${IMPACT_BATCH_SIZE:-4}
      IMPACT_POLL_INTERVAL_SECONDS: ${IMPACT_POLL_INTERVAL_SECONDS:-300}
      IMPACT_WINDOW_HOURS: ${IMPACT_WINDOW_HOURS:-168}
      IMPACT_CATEGORY_SLUG: ${IMPACT_CATEGORY_SLUG:-artificial-intelligence}
      IMPACT_RUN_UNTIL_EMPTY: ${IMPACT_RUN_UNTIL_EMPTY:-false}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "score:impact"]
    profiles:
      - jobs
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: "no"

  p0-briefing:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P0_BRIEFING_MODEL: ${P0_BRIEFING_MODEL:-gpt-5-nano}
      BRIEFING_OUTPUT_LANGUAGE: ${BRIEFING_OUTPUT_LANGUAGE:-Spanish}
      BRIEFING_VERSION_SUFFIX: ${BRIEFING_VERSION_SUFFIX:-}
      BRIEFING_CATEGORY_GUIDANCE_JSON: ${BRIEFING_CATEGORY_GUIDANCE_JSON:-}
      P0_BRIEFING_BATCH_SIZE: ${P0_BRIEFING_BATCH_SIZE:-10}
      P0_BRIEFING_POLL_INTERVAL_SECONDS: ${P0_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P0_BRIEFING_WINDOW_HOURS: ${P0_BRIEFING_WINDOW_HOURS:-168}
      P0_BRIEFING_CATEGORY_SLUG: ${P0_BRIEFING_CATEGORY_SLUG:-artificial-intelligence}
      P0_BRIEFING_RUN_UNTIL_EMPTY: ${P0_BRIEFING_RUN_UNTIL_EMPTY:-false}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "brief:p0"]
    profiles:
      - jobs
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: "no"

  p1-briefing:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P1_BRIEFING_MODEL: ${P1_BRIEFING_MODEL:-gpt-5-nano}
      BRIEFING_OUTPUT_LANGUAGE: ${BRIEFING_OUTPUT_LANGUAGE:-Spanish}
      BRIEFING_VERSION_SUFFIX: ${BRIEFING_VERSION_SUFFIX:-}
      BRIEFING_CATEGORY_GUIDANCE_JSON: ${BRIEFING_CATEGORY_GUIDANCE_JSON:-}
      P1_BRIEFING_BATCH_SIZE: ${P1_BRIEFING_BATCH_SIZE:-10}
      P1_BRIEFING_POLL_INTERVAL_SECONDS: ${P1_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P1_BRIEFING_WINDOW_HOURS: ${P1_BRIEFING_WINDOW_HOURS:-168}
      P1_BRIEFING_CATEGORY_SLUG: ${P1_BRIEFING_CATEGORY_SLUG:-artificial-intelligence}
      P1_BRIEFING_RUN_UNTIL_EMPTY: ${P1_BRIEFING_RUN_UNTIL_EMPTY:-false}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "brief:p1"]
    profiles:
      - jobs
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: "no"

  p2-briefing:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P2_BRIEFING_MODEL: ${P2_BRIEFING_MODEL:-gpt-5-nano}
      BRIEFING_OUTPUT_LANGUAGE: ${BRIEFING_OUTPUT_LANGUAGE:-Spanish}
      BRIEFING_VERSION_SUFFIX: ${BRIEFING_VERSION_SUFFIX:-}
      BRIEFING_CATEGORY_GUIDANCE_JSON: ${BRIEFING_CATEGORY_GUIDANCE_JSON:-}
      P2_BRIEFING_BATCH_SIZE: ${P2_BRIEFING_BATCH_SIZE:-20}
      P2_BRIEFING_POLL_INTERVAL_SECONDS: ${P2_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P2_BRIEFING_WINDOW_HOURS: ${P2_BRIEFING_WINDOW_HOURS:-168}
      P2_BRIEFING_CATEGORY_SLUG: ${P2_BRIEFING_CATEGORY_SLUG:-artificial-intelligence}
      P2_BRIEFING_RUN_UNTIL_EMPTY: ${P2_BRIEFING_RUN_UNTIL_EMPTY:-false}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "brief:p2"]
    profiles:
      - jobs
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: "no"

  p3-briefing:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P3_BRIEFING_MODEL: ${P3_BRIEFING_MODEL:-gpt-5-nano}
      BRIEFING_OUTPUT_LANGUAGE: ${BRIEFING_OUTPUT_LANGUAGE:-Spanish}
      BRIEFING_VERSION_SUFFIX: ${BRIEFING_VERSION_SUFFIX:-}
      BRIEFING_CATEGORY_GUIDANCE_JSON: ${BRIEFING_CATEGORY_GUIDANCE_JSON:-}
      P3_BRIEFING_BATCH_SIZE: ${P3_BRIEFING_BATCH_SIZE:-20}
      P3_BRIEFING_POLL_INTERVAL_SECONDS: ${P3_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P3_BRIEFING_WINDOW_HOURS: ${P3_BRIEFING_WINDOW_HOURS:-168}
      P3_BRIEFING_CATEGORY_SLUG: ${P3_BRIEFING_CATEGORY_SLUG:-artificial-intelligence}
      P3_BRIEFING_RUN_UNTIL_EMPTY: ${P3_BRIEFING_RUN_UNTIL_EMPTY:-false}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "brief:p3"]
    profiles:
      - jobs
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: "no"

  mattermost-notifier:
    <<: *app-hardening
    build: .
    dns:
      - 1.1.1.1
      - 8.8.8.8
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      RSS_PUBLIC_BASE_URL: ${RSS_PUBLIC_BASE_URL:-http://localhost:3000}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      MATTERMOST_WEBHOOK_URL: ${MATTERMOST_WEBHOOK_URL:-}
      MATTERMOST_ENABLED: ${MATTERMOST_ENABLED:-true}
      MATTERMOST_POLL_INTERVAL_SECONDS: ${MATTERMOST_POLL_INTERVAL_SECONDS:-60}
      MATTERMOST_BATCH_SIZE: ${MATTERMOST_BATCH_SIZE:-10}
      MATTERMOST_WINDOW_HOURS: ${MATTERMOST_WINDOW_HOURS:-168}
      MATTERMOST_STABILITY_DELAY_MINUTES: ${MATTERMOST_STABILITY_DELAY_MINUTES:-10}
      MATTERMOST_PROCESSING_STALE_MINUTES: ${MATTERMOST_PROCESSING_STALE_MINUTES:-10}
      MATTERMOST_FAILED_RETRY_BACKOFF_MINUTES: ${MATTERMOST_FAILED_RETRY_BACKOFF_MINUTES:-5}
      MATTERMOST_REQUIRE_CROSS_CATEGORY_CLEARANCE: ${MATTERMOST_REQUIRE_CROSS_CATEGORY_CLEARANCE:-true}
      MATTERMOST_CROSS_CATEGORY_CLEARANCE_MIN_SIMILARITY: ${MATTERMOST_CROSS_CATEGORY_CLEARANCE_MIN_SIMILARITY:-0.68}
      MATTERMOST_CATEGORY_SLUG: ${MATTERMOST_CATEGORY_SLUG:-artificial-intelligence}
      MATTERMOST_CATEGORY_SLUGS: ${MATTERMOST_CATEGORY_SLUGS:-artificial-intelligence}
      MATTERMOST_NOTIFY_EXISTING: ${MATTERMOST_NOTIFY_EXISTING:-false}
      MATTERMOST_LEVELS: ${MATTERMOST_LEVELS:-P0}
      MATTERMOST_USERNAME: ${MATTERMOST_USERNAME:-SignalRSS}
      MATTERMOST_ICON_EMOJI: ${MATTERMOST_ICON_EMOJI:-:satellite:}
      MATTERMOST_CHANNEL: ${MATTERMOST_CHANNEL:-}
      MATTERMOST_CHANNELS_BY_CATEGORY: ${MATTERMOST_CHANNELS_BY_CATEGORY:-}
      MATTERMOST_THUMBNAIL_MAX_CANDIDATES: ${MATTERMOST_THUMBNAIL_MAX_CANDIDATES:-12}
      MATTERMOST_THUMBNAIL_METADATA_MAX_BYTES: ${MATTERMOST_THUMBNAIL_METADATA_MAX_BYTES:-524288}
      MATTERMOST_THUMBNAIL_DOWNLOAD_MAX_BYTES: ${MATTERMOST_THUMBNAIL_DOWNLOAD_MAX_BYTES:-12582912}
      MATTERMOST_GENERATE_MISSING_THUMBNAILS: ${MATTERMOST_GENERATE_MISSING_THUMBNAILS:-true}
      MATTERMOST_GENERATED_THUMBNAIL_MODEL: ${MATTERMOST_GENERATED_THUMBNAIL_MODEL:-gpt-image-2}
      MATTERMOST_GENERATED_THUMBNAIL_SIZE: ${MATTERMOST_GENERATED_THUMBNAIL_SIZE:-1536x1024}
      MATTERMOST_GENERATED_THUMBNAIL_QUALITY: ${MATTERMOST_GENERATED_THUMBNAIL_QUALITY:-low}
      MATTERMOST_GENERATED_THUMBNAIL_TIMEOUT_MS: ${MATTERMOST_GENERATED_THUMBNAIL_TIMEOUT_MS:-45000}
      MATTERMOST_GENERATED_THUMBNAIL_ATTEMPTS: ${MATTERMOST_GENERATED_THUMBNAIL_ATTEMPTS:-3}
      MATTERMOST_GENERATED_THUMBNAIL_BACKOFF_MS: ${MATTERMOST_GENERATED_THUMBNAIL_BACKOFF_MS:-1000,3000,7000}
      MATTERMOST_IMAGE_PROVIDER: ${MATTERMOST_IMAGE_PROVIDER:-openai}
      MATTERMOST_IMAGE_PROVIDER_FALLBACKS: ${MATTERMOST_IMAGE_PROVIDER_FALLBACKS:-${MATTERMOST_IMAGE_PROVIDER:-openai}}
      MATTERMOST_IMAGE_OPENROUTER_API_KEY: ${MATTERMOST_IMAGE_OPENROUTER_API_KEY:-}
      MATTERMOST_IMAGE_OPENROUTER_MODEL: ${MATTERMOST_IMAGE_OPENROUTER_MODEL:-google/gemini-2.5-flash-image}
      MATTERMOST_IMAGE_OPENROUTER_REFERER: ${MATTERMOST_IMAGE_OPENROUTER_REFERER:-}
      MATTERMOST_IMAGE_OPENROUTER_TITLE: ${MATTERMOST_IMAGE_OPENROUTER_TITLE:-SignalRSS}
      MATTERMOST_IMAGE_NVIDIA_API_KEY: ${MATTERMOST_IMAGE_NVIDIA_API_KEY:-${NVIDIA_API_KEY:-}}
      MATTERMOST_IMAGE_NVIDIA_ENDPOINT: ${MATTERMOST_IMAGE_NVIDIA_ENDPOINT:-https://ai.api.nvidia.com/v1/genai/black-forest-labs/flux.2-klein-4b}
      MATTERMOST_IMAGE_NVIDIA_MODEL: ${MATTERMOST_IMAGE_NVIDIA_MODEL:-black-forest-labs/flux.2-klein-4b}
      MATTERMOST_IMAGE_NVIDIA_WIDTH: ${MATTERMOST_IMAGE_NVIDIA_WIDTH:-1344}
      MATTERMOST_IMAGE_NVIDIA_HEIGHT: ${MATTERMOST_IMAGE_NVIDIA_HEIGHT:-768}
      MATTERMOST_IMAGE_NVIDIA_STEPS: ${MATTERMOST_IMAGE_NVIDIA_STEPS:-4}
      MATTERMOST_GENERATED_THUMBNAIL_DIR: ${MATTERMOST_GENERATED_THUMBNAIL_DIR:-/app/data/generated-thumbnails}
      MATTERMOST_GENERATED_THUMBNAIL_PUBLIC_PATH: ${MATTERMOST_GENERATED_THUMBNAIL_PUBLIC_PATH:-/generated-thumbnails}
      MATTERMOST_IMAGE_UPLOAD_ENABLED: ${MATTERMOST_IMAGE_UPLOAD_ENABLED:-true}
      MATTERMOST_IMAGE_UPLOAD_PROVIDER: ${MATTERMOST_IMAGE_UPLOAD_PROVIDER:-litterbox}
      MATTERMOST_IMAGE_UPLOAD_ENDPOINT: ${MATTERMOST_IMAGE_UPLOAD_ENDPOINT:-https://litterbox.catbox.moe/resources/internals/api.php}
      MATTERMOST_IMAGE_UPLOAD_EXPIRATION: ${MATTERMOST_IMAGE_UPLOAD_EXPIRATION:-72h}
      MATTERMOST_IMAGE_UPLOAD_ATTEMPTS: ${MATTERMOST_IMAGE_UPLOAD_ATTEMPTS:-3}
      MATTERMOST_IMAGE_UPLOAD_BACKOFF_MS: ${MATTERMOST_IMAGE_UPLOAD_BACKOFF_MS:-1000,3000,7000}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "notify:mattermost"]
    volumes:
      - ./data:/app/data
    profiles:
      - jobs
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
    restart: "no"

  classifier-worker:
    <<: *app-hardening
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      CLASSIFIER_BATCH_SIZE: ${CLASSIFIER_BATCH_SIZE:-50}
      CLASSIFIER_POLL_INTERVAL_SECONDS: ${CLASSIFIER_POLL_INTERVAL_SECONDS:-300}
      CLASSIFIER_RUN_UNTIL_EMPTY: "true"
      CLASSIFIER_MAX_SUMMARY_CHARS: ${CLASSIFIER_MAX_SUMMARY_CHARS:-1000}
      CLASSIFIER_MAX_CONTENT_CHARS: ${CLASSIFIER_MAX_CONTENT_CHARS:-500}
      CLASSIFIER_MAX_INPUT_CHARS: ${CLASSIFIER_MAX_INPUT_CHARS:-3000}
      CLASSIFIER_TOP_CATEGORIES: ${CLASSIFIER_TOP_CATEGORIES:-3}
      CLASSIFIER_MIN_CONFIDENCE: ${CLASSIFIER_MIN_CONFIDENCE:-0.18}
      CLASSIFIER_AI_MIN_CONFIDENCE: ${CLASSIFIER_AI_MIN_CONFIDENCE:-0.30}
      CLASSIFIER_MIN_MARGIN: ${CLASSIFIER_MIN_MARGIN:-0}
      CLASSIFIER_MIN_CONFIDENCE_CLOUD_INFRASTRUCTURE: ${CLASSIFIER_MIN_CONFIDENCE_CLOUD_INFRASTRUCTURE:-0.26}
      CLASSIFIER_MIN_MARGIN_CLOUD_INFRASTRUCTURE: ${CLASSIFIER_MIN_MARGIN_CLOUD_INFRASTRUCTURE:-0.02}
      CLASSIFIER_MIN_CONFIDENCE_AUTOMOTIVE_MOBILITY: ${CLASSIFIER_MIN_CONFIDENCE_AUTOMOTIVE_MOBILITY:-0.29}
      CLASSIFIER_MIN_MARGIN_AUTOMOTIVE_MOBILITY: ${CLASSIFIER_MIN_MARGIN_AUTOMOTIVE_MOBILITY:-0.01}
      CLASSIFIER_MIN_CONFIDENCE_GAMING: ${CLASSIFIER_MIN_CONFIDENCE_GAMING:-0.24}
      CLASSIFIER_MIN_MARGIN_GAMING: ${CLASSIFIER_MIN_MARGIN_GAMING:-0.01}
      CLASSIFIER_MIN_CONFIDENCE_CRYPTO_WEB3: ${CLASSIFIER_MIN_CONFIDENCE_CRYPTO_WEB3:-0.34}
      CLASSIFIER_MIN_MARGIN_CRYPTO_WEB3: ${CLASSIFIER_MIN_MARGIN_CRYPTO_WEB3:-0.02}
      CLASSIFIER_MIN_CONFIDENCE_SOFTWARE_DEVELOPMENT: ${CLASSIFIER_MIN_CONFIDENCE_SOFTWARE_DEVELOPMENT:-0.22}
      CLASSIFIER_MIN_MARGIN_SOFTWARE_DEVELOPMENT: ${CLASSIFIER_MIN_MARGIN_SOFTWARE_DEVELOPMENT:-0.01}
      CLASSIFIER_GUARD_AUTOMOTIVE_MOBILITY_ENABLED: ${CLASSIFIER_GUARD_AUTOMOTIVE_MOBILITY_ENABLED:-true}
      CLASSIFIER_GUARD_AUTOMOTIVE_STRICT_EXCLUDES: ${CLASSIFIER_GUARD_AUTOMOTIVE_STRICT_EXCLUDES:-true}
      CLASSIFIER_GUARD_STARTUPS_VENTURE_CAPITAL_ENABLED: ${CLASSIFIER_GUARD_STARTUPS_VENTURE_CAPITAL_ENABLED:-true}
      CLASSIFIER_GUARD_STARTUPS_VENTURE_CAPITAL_STRICT_EXCLUDES: ${CLASSIFIER_GUARD_STARTUPS_VENTURE_CAPITAL_STRICT_EXCLUDES:-true}
      CLASSIFIER_GUARD_SCIENCE_RESEARCH_ENABLED: ${CLASSIFIER_GUARD_SCIENCE_RESEARCH_ENABLED:-true}
      CLASSIFIER_GUARD_SCIENCE_RESEARCH_STRICT_EXCLUDES: ${CLASSIFIER_GUARD_SCIENCE_RESEARCH_STRICT_EXCLUDES:-true}
      CLASSIFIER_GUARD_POLICY_REGULATION_ENABLED: ${CLASSIFIER_GUARD_POLICY_REGULATION_ENABLED:-true}
      CLASSIFIER_GUARD_POLICY_REGULATION_STRICT_EXCLUDES: ${CLASSIFIER_GUARD_POLICY_REGULATION_STRICT_EXCLUDES:-true}
      CLASSIFIER_GUARD_CYBERSECURITY_ENABLED: ${CLASSIFIER_GUARD_CYBERSECURITY_ENABLED:-true}
      CLASSIFIER_GUARD_CYBERSECURITY_STRICT_EXCLUDES: ${CLASSIFIER_GUARD_CYBERSECURITY_STRICT_EXCLUDES:-true}
      CLASSIFIER_GUARD_GAMING_ENABLED: ${CLASSIFIER_GUARD_GAMING_ENABLED:-true}
      CLASSIFIER_GUARD_GAMING_STRICT_EXCLUDES: ${CLASSIFIER_GUARD_GAMING_STRICT_EXCLUDES:-true}
      CLASSIFIER_GUARD_FINTECH_CRYPTO_ENABLED: ${CLASSIFIER_GUARD_FINTECH_CRYPTO_ENABLED:-true}
      CLASSIFIER_GUARD_FINTECH_CRYPTO_STRICT_EXCLUDES: ${CLASSIFIER_GUARD_FINTECH_CRYPTO_STRICT_EXCLUDES:-true}
      CLASSIFIER_GUARD_CRYPTO_WEB3_ENABLED: ${CLASSIFIER_GUARD_CRYPTO_WEB3_ENABLED:-true}
      CLASSIFIER_GUARD_CRYPTO_WEB3_STRICT_EXCLUDES: ${CLASSIFIER_GUARD_CRYPTO_WEB3_STRICT_EXCLUDES:-true}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "classifier:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  impact-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-${MATTERMOST_IMAGE_OPENROUTER_API_KEY:-}}
      OPENROUTER_REFERER: ${OPENROUTER_REFERER:-${MATTERMOST_IMAGE_OPENROUTER_REFERER:-}}
      OPENROUTER_TITLE: ${OPENROUTER_TITLE:-SignalRSS}
      OPENROUTER_REQUEST_TIMEOUT_MS: ${OPENROUTER_REQUEST_TIMEOUT_MS:-45000}
      GROQ_API_KEY: ${GROQ_API_KEY:-}
      GROQ_BASE_URL: ${GROQ_BASE_URL:-https://api.groq.com/openai/v1}
      GROQ_REQUEST_TIMEOUT_MS: ${GROQ_REQUEST_TIMEOUT_MS:-45000}
      NVIDIA_API_KEY: ${NVIDIA_API_KEY:-${MATTERMOST_IMAGE_NVIDIA_API_KEY:-}}
      NVIDIA_BASE_URL: ${NVIDIA_BASE_URL:-https://integrate.api.nvidia.com}
      NVIDIA_REQUEST_TIMEOUT_MS: ${NVIDIA_REQUEST_TIMEOUT_MS:-45000}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      IMPACT_MODEL: ${IMPACT_MODEL:-gpt-5-nano}
      IMPACT_RUBRIC_VERSION: ${IMPACT_RUBRIC_VERSION:-impact-v2}
      IMPACT_CATEGORY_GUIDANCE_JSON: ${IMPACT_CATEGORY_GUIDANCE_JSON:-}
      IMPACT_MAX_OUTPUT_TOKENS: ${IMPACT_MAX_OUTPUT_TOKENS:-2200}
      IMPACT_MODEL_FALLBACKS: ${IMPACT_MODEL_FALLBACKS:-nvidia:openai/gpt-oss-120b,groq:openai/gpt-oss-120b,openrouter:openai/gpt-oss-120b:free,groq:llama-3.3-70b-versatile,groq:openai/gpt-oss-20b,openrouter:nvidia/nemotron-3-super-120b-a12b:free,openai:gpt-5-nano}
      IMPACT_BATCH_SIZE: ${IMPACT_BATCH_SIZE:-4}
      IMPACT_POLL_INTERVAL_SECONDS: ${IMPACT_POLL_INTERVAL_SECONDS:-300}
      IMPACT_WINDOW_HOURS: ${IMPACT_WINDOW_HOURS:-168}
      IMPACT_CATEGORY_SLUG: ${IMPACT_CATEGORY_SLUG:-artificial-intelligence}
      IMPACT_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "impact:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cluster-merge-worker:
    <<: *app-hardening
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      CLUSTER_MERGE_BATCH_SIZE: ${CLUSTER_MERGE_BATCH_SIZE:-50}
      CLUSTER_MERGE_POLL_INTERVAL_SECONDS: ${CLUSTER_MERGE_POLL_INTERVAL_SECONDS:-300}
      CLUSTER_MERGE_SIMILARITY_THRESHOLD: ${CLUSTER_MERGE_SIMILARITY_THRESHOLD:-0.82}
      CLUSTER_MERGE_CATEGORY_SLUG: ${CLUSTER_MERGE_CATEGORY_SLUG:-artificial-intelligence}
      CLUSTER_MERGE_WINDOW_DAYS: ${CLUSTER_MERGE_WINDOW_DAYS:-7}
      CLUSTER_MERGE_RUN_UNTIL_STABLE: "true"
    command: ["npm", "run", "cluster-merge:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  p0-briefing-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P0_BRIEFING_MODEL: ${P0_BRIEFING_MODEL:-gpt-5-nano}
      P0_BRIEFING_BATCH_SIZE: ${P0_BRIEFING_BATCH_SIZE:-10}
      P0_BRIEFING_POLL_INTERVAL_SECONDS: ${P0_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P0_BRIEFING_WINDOW_HOURS: ${P0_BRIEFING_WINDOW_HOURS:-168}
      P0_BRIEFING_CATEGORY_SLUG: ${P0_BRIEFING_CATEGORY_SLUG:-artificial-intelligence}
      P0_BRIEFING_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p0-briefing:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  p1-briefing-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P1_BRIEFING_MODEL: ${P1_BRIEFING_MODEL:-gpt-5-nano}
      P1_BRIEFING_BATCH_SIZE: ${P1_BRIEFING_BATCH_SIZE:-10}
      P1_BRIEFING_POLL_INTERVAL_SECONDS: ${P1_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P1_BRIEFING_WINDOW_HOURS: ${P1_BRIEFING_WINDOW_HOURS:-168}
      P1_BRIEFING_CATEGORY_SLUG: ${P1_BRIEFING_CATEGORY_SLUG:-artificial-intelligence}
      P1_BRIEFING_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p1-briefing:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  p2-briefing-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P2_BRIEFING_MODEL: ${P2_BRIEFING_MODEL:-gpt-5-nano}
      P2_BRIEFING_BATCH_SIZE: ${P2_BRIEFING_BATCH_SIZE:-20}
      P2_BRIEFING_POLL_INTERVAL_SECONDS: ${P2_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P2_BRIEFING_WINDOW_HOURS: ${P2_BRIEFING_WINDOW_HOURS:-168}
      P2_BRIEFING_CATEGORY_SLUG: ${P2_BRIEFING_CATEGORY_SLUG:-artificial-intelligence}
      P2_BRIEFING_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p2-briefing:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  p3-briefing-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P3_BRIEFING_MODEL: ${P3_BRIEFING_MODEL:-gpt-5-nano}
      P3_BRIEFING_BATCH_SIZE: ${P3_BRIEFING_BATCH_SIZE:-20}
      P3_BRIEFING_POLL_INTERVAL_SECONDS: ${P3_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P3_BRIEFING_WINDOW_HOURS: ${P3_BRIEFING_WINDOW_HOURS:-168}
      P3_BRIEFING_CATEGORY_SLUG: ${P3_BRIEFING_CATEGORY_SLUG:-artificial-intelligence}
      P3_BRIEFING_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p3-briefing:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  mattermost-worker:
    <<: *app-hardening
    build: .
    dns:
      - 1.1.1.1
      - 8.8.8.8
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      RSS_PUBLIC_BASE_URL: ${RSS_PUBLIC_BASE_URL:-http://localhost:3000}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      MATTERMOST_WEBHOOK_URL: ${MATTERMOST_WEBHOOK_URL:-}
      MATTERMOST_ENABLED: ${MATTERMOST_ENABLED:-true}
      MATTERMOST_POLL_INTERVAL_SECONDS: ${MATTERMOST_POLL_INTERVAL_SECONDS:-60}
      MATTERMOST_BATCH_SIZE: ${MATTERMOST_BATCH_SIZE:-10}
      MATTERMOST_WINDOW_HOURS: ${MATTERMOST_WINDOW_HOURS:-168}
      MATTERMOST_STABILITY_DELAY_MINUTES: ${MATTERMOST_STABILITY_DELAY_MINUTES:-10}
      MATTERMOST_PROCESSING_STALE_MINUTES: ${MATTERMOST_PROCESSING_STALE_MINUTES:-10}
      MATTERMOST_FAILED_RETRY_BACKOFF_MINUTES: ${MATTERMOST_FAILED_RETRY_BACKOFF_MINUTES:-5}
      MATTERMOST_REQUIRE_CROSS_CATEGORY_CLEARANCE: ${MATTERMOST_REQUIRE_CROSS_CATEGORY_CLEARANCE:-true}
      MATTERMOST_CROSS_CATEGORY_CLEARANCE_MIN_SIMILARITY: ${MATTERMOST_CROSS_CATEGORY_CLEARANCE_MIN_SIMILARITY:-0.68}
      MATTERMOST_CATEGORY_SLUG: ${MATTERMOST_CATEGORY_SLUG:-artificial-intelligence}
      MATTERMOST_CATEGORY_SLUGS: ${MATTERMOST_CATEGORY_SLUGS:-artificial-intelligence}
      MATTERMOST_NOTIFY_EXISTING: ${MATTERMOST_NOTIFY_EXISTING:-false}
      MATTERMOST_LEVELS: ${MATTERMOST_LEVELS:-P0}
      MATTERMOST_USERNAME: ${MATTERMOST_USERNAME:-SignalRSS}
      MATTERMOST_ICON_EMOJI: ${MATTERMOST_ICON_EMOJI:-:satellite:}
      MATTERMOST_CHANNEL: ${MATTERMOST_CHANNEL:-}
      MATTERMOST_CHANNELS_BY_CATEGORY: ${MATTERMOST_CHANNELS_BY_CATEGORY:-}
      MATTERMOST_THUMBNAIL_MAX_CANDIDATES: ${MATTERMOST_THUMBNAIL_MAX_CANDIDATES:-12}
      MATTERMOST_THUMBNAIL_METADATA_MAX_BYTES: ${MATTERMOST_THUMBNAIL_METADATA_MAX_BYTES:-524288}
      MATTERMOST_THUMBNAIL_DOWNLOAD_MAX_BYTES: ${MATTERMOST_THUMBNAIL_DOWNLOAD_MAX_BYTES:-12582912}
      MATTERMOST_GENERATE_MISSING_THUMBNAILS: ${MATTERMOST_GENERATE_MISSING_THUMBNAILS:-true}
      MATTERMOST_GENERATED_THUMBNAIL_MODEL: ${MATTERMOST_GENERATED_THUMBNAIL_MODEL:-gpt-image-2}
      MATTERMOST_GENERATED_THUMBNAIL_SIZE: ${MATTERMOST_GENERATED_THUMBNAIL_SIZE:-1536x1024}
      MATTERMOST_GENERATED_THUMBNAIL_QUALITY: ${MATTERMOST_GENERATED_THUMBNAIL_QUALITY:-low}
      MATTERMOST_GENERATED_THUMBNAIL_TIMEOUT_MS: ${MATTERMOST_GENERATED_THUMBNAIL_TIMEOUT_MS:-45000}
      MATTERMOST_GENERATED_THUMBNAIL_ATTEMPTS: ${MATTERMOST_GENERATED_THUMBNAIL_ATTEMPTS:-3}
      MATTERMOST_GENERATED_THUMBNAIL_BACKOFF_MS: ${MATTERMOST_GENERATED_THUMBNAIL_BACKOFF_MS:-1000,3000,7000}
      MATTERMOST_IMAGE_PROVIDER: ${MATTERMOST_IMAGE_PROVIDER:-openai}
      MATTERMOST_IMAGE_PROVIDER_FALLBACKS: ${MATTERMOST_IMAGE_PROVIDER_FALLBACKS:-${MATTERMOST_IMAGE_PROVIDER:-openai}}
      MATTERMOST_IMAGE_OPENROUTER_API_KEY: ${MATTERMOST_IMAGE_OPENROUTER_API_KEY:-}
      MATTERMOST_IMAGE_OPENROUTER_MODEL: ${MATTERMOST_IMAGE_OPENROUTER_MODEL:-google/gemini-2.5-flash-image}
      MATTERMOST_IMAGE_OPENROUTER_REFERER: ${MATTERMOST_IMAGE_OPENROUTER_REFERER:-}
      MATTERMOST_IMAGE_OPENROUTER_TITLE: ${MATTERMOST_IMAGE_OPENROUTER_TITLE:-SignalRSS}
      MATTERMOST_IMAGE_NVIDIA_API_KEY: ${MATTERMOST_IMAGE_NVIDIA_API_KEY:-${NVIDIA_API_KEY:-}}
      MATTERMOST_IMAGE_NVIDIA_ENDPOINT: ${MATTERMOST_IMAGE_NVIDIA_ENDPOINT:-https://ai.api.nvidia.com/v1/genai/black-forest-labs/flux.2-klein-4b}
      MATTERMOST_IMAGE_NVIDIA_MODEL: ${MATTERMOST_IMAGE_NVIDIA_MODEL:-black-forest-labs/flux.2-klein-4b}
      MATTERMOST_IMAGE_NVIDIA_WIDTH: ${MATTERMOST_IMAGE_NVIDIA_WIDTH:-1344}
      MATTERMOST_IMAGE_NVIDIA_HEIGHT: ${MATTERMOST_IMAGE_NVIDIA_HEIGHT:-768}
      MATTERMOST_IMAGE_NVIDIA_STEPS: ${MATTERMOST_IMAGE_NVIDIA_STEPS:-4}
      MATTERMOST_GENERATED_THUMBNAIL_DIR: ${MATTERMOST_GENERATED_THUMBNAIL_DIR:-/app/data/generated-thumbnails}
      MATTERMOST_GENERATED_THUMBNAIL_PUBLIC_PATH: ${MATTERMOST_GENERATED_THUMBNAIL_PUBLIC_PATH:-/generated-thumbnails}
      MATTERMOST_IMAGE_UPLOAD_ENABLED: ${MATTERMOST_IMAGE_UPLOAD_ENABLED:-true}
      MATTERMOST_IMAGE_UPLOAD_PROVIDER: ${MATTERMOST_IMAGE_UPLOAD_PROVIDER:-litterbox}
      MATTERMOST_IMAGE_UPLOAD_ENDPOINT: ${MATTERMOST_IMAGE_UPLOAD_ENDPOINT:-https://litterbox.catbox.moe/resources/internals/api.php}
      MATTERMOST_IMAGE_UPLOAD_EXPIRATION: ${MATTERMOST_IMAGE_UPLOAD_EXPIRATION:-72h}
      MATTERMOST_IMAGE_UPLOAD_ATTEMPTS: ${MATTERMOST_IMAGE_UPLOAD_ATTEMPTS:-3}
      MATTERMOST_IMAGE_UPLOAD_BACKOFF_MS: ${MATTERMOST_IMAGE_UPLOAD_BACKOFF_MS:-1000,3000,7000}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "mattermost:worker"]
    volumes:
      - ./data:/app/data
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
    restart: unless-stopped

  cybersecurity-cluster-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      CLUSTER_BATCH_SIZE: ${CYBERSECURITY_CLUSTER_BATCH_SIZE:-200}
      CLUSTER_POLL_INTERVAL_SECONDS: ${CYBERSECURITY_CLUSTER_POLL_INTERVAL_SECONDS:-300}
      CLUSTER_SIMILARITY_THRESHOLD: ${CYBERSECURITY_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CLUSTER_CATEGORY_SLUG: cybersecurity
      CLUSTER_WINDOW_DAYS: ${CYBERSECURITY_CLUSTER_WINDOW_DAYS:-7}
      CLUSTER_RUN_UNTIL_EMPTY: "true"
    command: ["npm", "run", "cluster:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cybersecurity-impact-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-${MATTERMOST_IMAGE_OPENROUTER_API_KEY:-}}
      OPENROUTER_REFERER: ${OPENROUTER_REFERER:-${MATTERMOST_IMAGE_OPENROUTER_REFERER:-}}
      OPENROUTER_TITLE: ${OPENROUTER_TITLE:-SignalRSS}
      OPENROUTER_REQUEST_TIMEOUT_MS: ${OPENROUTER_REQUEST_TIMEOUT_MS:-45000}
      GROQ_API_KEY: ${GROQ_API_KEY:-}
      GROQ_BASE_URL: ${GROQ_BASE_URL:-https://api.groq.com/openai/v1}
      GROQ_REQUEST_TIMEOUT_MS: ${GROQ_REQUEST_TIMEOUT_MS:-45000}
      NVIDIA_API_KEY: ${NVIDIA_API_KEY:-${MATTERMOST_IMAGE_NVIDIA_API_KEY:-}}
      NVIDIA_BASE_URL: ${NVIDIA_BASE_URL:-https://integrate.api.nvidia.com}
      NVIDIA_REQUEST_TIMEOUT_MS: ${NVIDIA_REQUEST_TIMEOUT_MS:-45000}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      IMPACT_MODEL: ${CYBERSECURITY_IMPACT_MODEL:-${IMPACT_MODEL:-gpt-5-nano}}
      IMPACT_RUBRIC_VERSION: ${IMPACT_RUBRIC_VERSION:-impact-v2}
      IMPACT_CATEGORY_GUIDANCE_JSON: ${IMPACT_CATEGORY_GUIDANCE_JSON:-}
      IMPACT_MAX_OUTPUT_TOKENS: ${IMPACT_MAX_OUTPUT_TOKENS:-2200}
      IMPACT_MODEL_FALLBACKS: ${IMPACT_MODEL_FALLBACKS:-nvidia:openai/gpt-oss-120b,groq:openai/gpt-oss-120b,openrouter:openai/gpt-oss-120b:free,groq:llama-3.3-70b-versatile,groq:openai/gpt-oss-20b,openrouter:nvidia/nemotron-3-super-120b-a12b:free,openai:gpt-5-nano}
      IMPACT_BATCH_SIZE: ${CYBERSECURITY_IMPACT_BATCH_SIZE:-1}
      IMPACT_POLL_INTERVAL_SECONDS: ${CYBERSECURITY_IMPACT_POLL_INTERVAL_SECONDS:-300}
      IMPACT_WINDOW_HOURS: ${CYBERSECURITY_IMPACT_WINDOW_HOURS:-168}
      IMPACT_CATEGORY_SLUG: cybersecurity
      IMPACT_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "impact:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cybersecurity-p0-adjudication-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P0_ADJUDICATION_MODEL: ${CYBERSECURITY_P0_ADJUDICATION_MODEL:-${P0_ADJUDICATION_MODEL:-gpt-5-nano}}
      P0_ADJUDICATION_BATCH_SIZE: ${CYBERSECURITY_P0_ADJUDICATION_BATCH_SIZE:-12}
      P0_ADJUDICATION_POLL_INTERVAL_SECONDS: ${CYBERSECURITY_P0_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P0_ADJUDICATION_WINDOW_HOURS: ${CYBERSECURITY_P0_ADJUDICATION_WINDOW_HOURS:-168}
      P0_ADJUDICATION_CATEGORY_SLUG: cybersecurity
      P0_ADJUDICATION_MIN_CENTROID_SIMILARITY: ${CYBERSECURITY_P0_ADJUDICATION_MIN_CENTROID_SIMILARITY:-0.70}
      P0_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P0_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P0_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${CYBERSECURITY_P0_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.72}
      P0_ADJUDICATION_MERGE_CONFIDENCE: ${P0_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p0-adjudication:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cybersecurity-p1-adjudication-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P1_ADJUDICATION_MODEL: ${CYBERSECURITY_P1_ADJUDICATION_MODEL:-${P1_ADJUDICATION_MODEL:-gpt-5-nano}}
      P1_ADJUDICATION_BATCH_SIZE: ${CYBERSECURITY_P1_ADJUDICATION_BATCH_SIZE:-20}
      P1_ADJUDICATION_POLL_INTERVAL_SECONDS: ${CYBERSECURITY_P1_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P1_ADJUDICATION_WINDOW_HOURS: ${CYBERSECURITY_P1_ADJUDICATION_WINDOW_HOURS:-168}
      P1_ADJUDICATION_CATEGORY_SLUG: cybersecurity
      P1_ADJUDICATION_P1P1_MIN_CENTROID_SIMILARITY: ${CYBERSECURITY_P1_ADJUDICATION_P1P1_MIN_CENTROID_SIMILARITY:-0.72}
      P1_ADJUDICATION_P1P0_MIN_CENTROID_SIMILARITY: ${CYBERSECURITY_P1_ADJUDICATION_P1P0_MIN_CENTROID_SIMILARITY:-0.70}
      P1_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P1_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P1_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${CYBERSECURITY_P1_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.74}
      P1_ADJUDICATION_MERGE_CONFIDENCE: ${P1_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p1-adjudication:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cybersecurity-p2-adjudication-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P2_ADJUDICATION_MODEL: ${CYBERSECURITY_P2_ADJUDICATION_MODEL:-${P2_ADJUDICATION_MODEL:-gpt-5-nano}}
      P2_ADJUDICATION_BATCH_SIZE: ${CYBERSECURITY_P2_ADJUDICATION_BATCH_SIZE:-20}
      P2_ADJUDICATION_POLL_INTERVAL_SECONDS: ${CYBERSECURITY_P2_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P2_ADJUDICATION_WINDOW_HOURS: ${CYBERSECURITY_P2_ADJUDICATION_WINDOW_HOURS:-168}
      P2_ADJUDICATION_CATEGORY_SLUG: cybersecurity
      P2_ADJUDICATION_P2P2_MIN_CENTROID_SIMILARITY: ${CYBERSECURITY_P2_ADJUDICATION_P2P2_MIN_CENTROID_SIMILARITY:-0.72}
      P2_ADJUDICATION_P2P1_MIN_CENTROID_SIMILARITY: ${CYBERSECURITY_P2_ADJUDICATION_P2P1_MIN_CENTROID_SIMILARITY:-0.70}
      P2_ADJUDICATION_P2P0_MIN_CENTROID_SIMILARITY: ${CYBERSECURITY_P2_ADJUDICATION_P2P0_MIN_CENTROID_SIMILARITY:-0.70}
      P2_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P2_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P2_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${CYBERSECURITY_P2_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.72}
      P2_ADJUDICATION_MERGE_CONFIDENCE: ${P2_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p2-adjudication:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cybersecurity-p3-adjudication-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P3_ADJUDICATION_MODEL: ${CYBERSECURITY_P3_ADJUDICATION_MODEL:-${P3_ADJUDICATION_MODEL:-gpt-5-nano}}
      P3_ADJUDICATION_BATCH_SIZE: ${CYBERSECURITY_P3_ADJUDICATION_BATCH_SIZE:-20}
      P3_ADJUDICATION_POLL_INTERVAL_SECONDS: ${CYBERSECURITY_P3_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P3_ADJUDICATION_WINDOW_HOURS: ${CYBERSECURITY_P3_ADJUDICATION_WINDOW_HOURS:-168}
      P3_ADJUDICATION_CATEGORY_SLUG: cybersecurity
      P3_ADJUDICATION_P3P3_MIN_CENTROID_SIMILARITY: ${CYBERSECURITY_P3_ADJUDICATION_P3P3_MIN_CENTROID_SIMILARITY:-0.72}
      P3_ADJUDICATION_P3P2_MIN_CENTROID_SIMILARITY: ${CYBERSECURITY_P3_ADJUDICATION_P3P2_MIN_CENTROID_SIMILARITY:-0.70}
      P3_ADJUDICATION_P3P1_MIN_CENTROID_SIMILARITY: ${CYBERSECURITY_P3_ADJUDICATION_P3P1_MIN_CENTROID_SIMILARITY:-0.70}
      P3_ADJUDICATION_P3P0_MIN_CENTROID_SIMILARITY: ${CYBERSECURITY_P3_ADJUDICATION_P3P0_MIN_CENTROID_SIMILARITY:-0.70}
      P3_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P3_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P3_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${CYBERSECURITY_P3_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.72}
      P3_ADJUDICATION_MERGE_CONFIDENCE: ${P3_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p3-adjudication:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cybersecurity-p0-briefing-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P0_BRIEFING_MODEL: ${CYBERSECURITY_P0_BRIEFING_MODEL:-${P0_BRIEFING_MODEL:-gpt-5-nano}}
      P0_BRIEFING_BATCH_SIZE: ${CYBERSECURITY_P0_BRIEFING_BATCH_SIZE:-10}
      P0_BRIEFING_POLL_INTERVAL_SECONDS: ${CYBERSECURITY_P0_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P0_BRIEFING_WINDOW_HOURS: ${CYBERSECURITY_P0_BRIEFING_WINDOW_HOURS:-168}
      P0_BRIEFING_CATEGORY_SLUG: cybersecurity
      P0_BRIEFING_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p0-briefing:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cybersecurity-p1-briefing-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P1_BRIEFING_MODEL: ${CYBERSECURITY_P1_BRIEFING_MODEL:-${P1_BRIEFING_MODEL:-gpt-5-nano}}
      P1_BRIEFING_BATCH_SIZE: ${CYBERSECURITY_P1_BRIEFING_BATCH_SIZE:-10}
      P1_BRIEFING_POLL_INTERVAL_SECONDS: ${CYBERSECURITY_P1_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P1_BRIEFING_WINDOW_HOURS: ${CYBERSECURITY_P1_BRIEFING_WINDOW_HOURS:-168}
      P1_BRIEFING_CATEGORY_SLUG: cybersecurity
      P1_BRIEFING_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p1-briefing:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cybersecurity-p2-briefing-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P2_BRIEFING_MODEL: ${CYBERSECURITY_P2_BRIEFING_MODEL:-${P2_BRIEFING_MODEL:-gpt-5-nano}}
      P2_BRIEFING_BATCH_SIZE: ${CYBERSECURITY_P2_BRIEFING_BATCH_SIZE:-20}
      P2_BRIEFING_POLL_INTERVAL_SECONDS: ${CYBERSECURITY_P2_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P2_BRIEFING_WINDOW_HOURS: ${CYBERSECURITY_P2_BRIEFING_WINDOW_HOURS:-168}
      P2_BRIEFING_CATEGORY_SLUG: cybersecurity
      P2_BRIEFING_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p2-briefing:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cybersecurity-p3-briefing-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P3_BRIEFING_MODEL: ${CYBERSECURITY_P3_BRIEFING_MODEL:-${P3_BRIEFING_MODEL:-gpt-5-nano}}
      P3_BRIEFING_BATCH_SIZE: ${CYBERSECURITY_P3_BRIEFING_BATCH_SIZE:-20}
      P3_BRIEFING_POLL_INTERVAL_SECONDS: ${CYBERSECURITY_P3_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P3_BRIEFING_WINDOW_HOURS: ${CYBERSECURITY_P3_BRIEFING_WINDOW_HOURS:-168}
      P3_BRIEFING_CATEGORY_SLUG: cybersecurity
      P3_BRIEFING_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p3-briefing:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cloud-infrastructure-cluster-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      CLUSTER_BATCH_SIZE: ${CLOUD_INFRASTRUCTURE_CLUSTER_BATCH_SIZE:-200}
      CLUSTER_POLL_INTERVAL_SECONDS: ${CLOUD_INFRASTRUCTURE_CLUSTER_POLL_INTERVAL_SECONDS:-300}
      CLUSTER_SIMILARITY_THRESHOLD: ${CLOUD_INFRASTRUCTURE_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CLUSTER_CATEGORY_SLUG: cloud-infrastructure
      CLUSTER_WINDOW_DAYS: ${CLOUD_INFRASTRUCTURE_CLUSTER_WINDOW_DAYS:-7}
      CLUSTER_RUN_UNTIL_EMPTY: "true"
    command: ["npm", "run", "cluster:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cloud-infrastructure-impact-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-${MATTERMOST_IMAGE_OPENROUTER_API_KEY:-}}
      OPENROUTER_REFERER: ${OPENROUTER_REFERER:-${MATTERMOST_IMAGE_OPENROUTER_REFERER:-}}
      OPENROUTER_TITLE: ${OPENROUTER_TITLE:-SignalRSS}
      OPENROUTER_REQUEST_TIMEOUT_MS: ${OPENROUTER_REQUEST_TIMEOUT_MS:-45000}
      GROQ_API_KEY: ${GROQ_API_KEY:-}
      GROQ_BASE_URL: ${GROQ_BASE_URL:-https://api.groq.com/openai/v1}
      GROQ_REQUEST_TIMEOUT_MS: ${GROQ_REQUEST_TIMEOUT_MS:-45000}
      NVIDIA_API_KEY: ${NVIDIA_API_KEY:-${MATTERMOST_IMAGE_NVIDIA_API_KEY:-}}
      NVIDIA_BASE_URL: ${NVIDIA_BASE_URL:-https://integrate.api.nvidia.com}
      NVIDIA_REQUEST_TIMEOUT_MS: ${NVIDIA_REQUEST_TIMEOUT_MS:-45000}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      IMPACT_MODEL: ${CLOUD_INFRASTRUCTURE_IMPACT_MODEL:-${IMPACT_MODEL:-gpt-5-nano}}
      IMPACT_RUBRIC_VERSION: ${IMPACT_RUBRIC_VERSION:-impact-v2}
      IMPACT_CATEGORY_GUIDANCE_JSON: ${IMPACT_CATEGORY_GUIDANCE_JSON:-}
      IMPACT_MAX_OUTPUT_TOKENS: ${IMPACT_MAX_OUTPUT_TOKENS:-2200}
      IMPACT_MODEL_FALLBACKS: ${IMPACT_MODEL_FALLBACKS:-nvidia:openai/gpt-oss-120b,groq:openai/gpt-oss-120b,openrouter:openai/gpt-oss-120b:free,groq:llama-3.3-70b-versatile,groq:openai/gpt-oss-20b,openrouter:nvidia/nemotron-3-super-120b-a12b:free,openai:gpt-5-nano}
      IMPACT_BATCH_SIZE: ${CLOUD_INFRASTRUCTURE_IMPACT_BATCH_SIZE:-1}
      IMPACT_POLL_INTERVAL_SECONDS: ${CLOUD_INFRASTRUCTURE_IMPACT_POLL_INTERVAL_SECONDS:-300}
      IMPACT_WINDOW_HOURS: ${CLOUD_INFRASTRUCTURE_IMPACT_WINDOW_HOURS:-168}
      IMPACT_CATEGORY_SLUG: cloud-infrastructure
      IMPACT_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "impact:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cloud-infrastructure-p0-adjudication-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P0_ADJUDICATION_MODEL: ${CLOUD_INFRASTRUCTURE_P0_ADJUDICATION_MODEL:-${P0_ADJUDICATION_MODEL:-gpt-5-nano}}
      P0_ADJUDICATION_BATCH_SIZE: ${CLOUD_INFRASTRUCTURE_P0_ADJUDICATION_BATCH_SIZE:-12}
      P0_ADJUDICATION_POLL_INTERVAL_SECONDS: ${CLOUD_INFRASTRUCTURE_P0_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P0_ADJUDICATION_WINDOW_HOURS: ${CLOUD_INFRASTRUCTURE_P0_ADJUDICATION_WINDOW_HOURS:-168}
      P0_ADJUDICATION_CATEGORY_SLUG: cloud-infrastructure
      P0_ADJUDICATION_MIN_CENTROID_SIMILARITY: ${CLOUD_INFRASTRUCTURE_P0_ADJUDICATION_MIN_CENTROID_SIMILARITY:-0.70}
      P0_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P0_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P0_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${CLOUD_INFRASTRUCTURE_P0_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.72}
      P0_ADJUDICATION_MERGE_CONFIDENCE: ${P0_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p0-adjudication:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cloud-infrastructure-p1-adjudication-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P1_ADJUDICATION_MODEL: ${CLOUD_INFRASTRUCTURE_P1_ADJUDICATION_MODEL:-${P1_ADJUDICATION_MODEL:-gpt-5-nano}}
      P1_ADJUDICATION_BATCH_SIZE: ${CLOUD_INFRASTRUCTURE_P1_ADJUDICATION_BATCH_SIZE:-20}
      P1_ADJUDICATION_POLL_INTERVAL_SECONDS: ${CLOUD_INFRASTRUCTURE_P1_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P1_ADJUDICATION_WINDOW_HOURS: ${CLOUD_INFRASTRUCTURE_P1_ADJUDICATION_WINDOW_HOURS:-168}
      P1_ADJUDICATION_CATEGORY_SLUG: cloud-infrastructure
      P1_ADJUDICATION_P1P1_MIN_CENTROID_SIMILARITY: ${CLOUD_INFRASTRUCTURE_P1_ADJUDICATION_P1P1_MIN_CENTROID_SIMILARITY:-0.72}
      P1_ADJUDICATION_P1P0_MIN_CENTROID_SIMILARITY: ${CLOUD_INFRASTRUCTURE_P1_ADJUDICATION_P1P0_MIN_CENTROID_SIMILARITY:-0.70}
      P1_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P1_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P1_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${CLOUD_INFRASTRUCTURE_P1_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.74}
      P1_ADJUDICATION_MERGE_CONFIDENCE: ${P1_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p1-adjudication:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cloud-infrastructure-p2-adjudication-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P2_ADJUDICATION_MODEL: ${CLOUD_INFRASTRUCTURE_P2_ADJUDICATION_MODEL:-${P2_ADJUDICATION_MODEL:-gpt-5-nano}}
      P2_ADJUDICATION_BATCH_SIZE: ${CLOUD_INFRASTRUCTURE_P2_ADJUDICATION_BATCH_SIZE:-20}
      P2_ADJUDICATION_POLL_INTERVAL_SECONDS: ${CLOUD_INFRASTRUCTURE_P2_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P2_ADJUDICATION_WINDOW_HOURS: ${CLOUD_INFRASTRUCTURE_P2_ADJUDICATION_WINDOW_HOURS:-168}
      P2_ADJUDICATION_CATEGORY_SLUG: cloud-infrastructure
      P2_ADJUDICATION_P2P2_MIN_CENTROID_SIMILARITY: ${CLOUD_INFRASTRUCTURE_P2_ADJUDICATION_P2P2_MIN_CENTROID_SIMILARITY:-0.72}
      P2_ADJUDICATION_P2P1_MIN_CENTROID_SIMILARITY: ${CLOUD_INFRASTRUCTURE_P2_ADJUDICATION_P2P1_MIN_CENTROID_SIMILARITY:-0.70}
      P2_ADJUDICATION_P2P0_MIN_CENTROID_SIMILARITY: ${CLOUD_INFRASTRUCTURE_P2_ADJUDICATION_P2P0_MIN_CENTROID_SIMILARITY:-0.70}
      P2_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P2_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P2_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${CLOUD_INFRASTRUCTURE_P2_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.72}
      P2_ADJUDICATION_MERGE_CONFIDENCE: ${P2_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p2-adjudication:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cloud-infrastructure-p3-adjudication-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P3_ADJUDICATION_MODEL: ${CLOUD_INFRASTRUCTURE_P3_ADJUDICATION_MODEL:-${P3_ADJUDICATION_MODEL:-gpt-5-nano}}
      P3_ADJUDICATION_BATCH_SIZE: ${CLOUD_INFRASTRUCTURE_P3_ADJUDICATION_BATCH_SIZE:-20}
      P3_ADJUDICATION_POLL_INTERVAL_SECONDS: ${CLOUD_INFRASTRUCTURE_P3_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P3_ADJUDICATION_WINDOW_HOURS: ${CLOUD_INFRASTRUCTURE_P3_ADJUDICATION_WINDOW_HOURS:-168}
      P3_ADJUDICATION_CATEGORY_SLUG: cloud-infrastructure
      P3_ADJUDICATION_P3P3_MIN_CENTROID_SIMILARITY: ${CLOUD_INFRASTRUCTURE_P3_ADJUDICATION_P3P3_MIN_CENTROID_SIMILARITY:-0.72}
      P3_ADJUDICATION_P3P2_MIN_CENTROID_SIMILARITY: ${CLOUD_INFRASTRUCTURE_P3_ADJUDICATION_P3P2_MIN_CENTROID_SIMILARITY:-0.70}
      P3_ADJUDICATION_P3P1_MIN_CENTROID_SIMILARITY: ${CLOUD_INFRASTRUCTURE_P3_ADJUDICATION_P3P1_MIN_CENTROID_SIMILARITY:-0.70}
      P3_ADJUDICATION_P3P0_MIN_CENTROID_SIMILARITY: ${CLOUD_INFRASTRUCTURE_P3_ADJUDICATION_P3P0_MIN_CENTROID_SIMILARITY:-0.70}
      P3_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P3_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P3_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${CLOUD_INFRASTRUCTURE_P3_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.72}
      P3_ADJUDICATION_MERGE_CONFIDENCE: ${P3_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p3-adjudication:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cloud-infrastructure-p0-briefing-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P0_BRIEFING_MODEL: ${CLOUD_INFRASTRUCTURE_P0_BRIEFING_MODEL:-${P0_BRIEFING_MODEL:-gpt-5-nano}}
      P0_BRIEFING_BATCH_SIZE: ${CLOUD_INFRASTRUCTURE_P0_BRIEFING_BATCH_SIZE:-10}
      P0_BRIEFING_POLL_INTERVAL_SECONDS: ${CLOUD_INFRASTRUCTURE_P0_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P0_BRIEFING_WINDOW_HOURS: ${CLOUD_INFRASTRUCTURE_P0_BRIEFING_WINDOW_HOURS:-168}
      P0_BRIEFING_CATEGORY_SLUG: cloud-infrastructure
      P0_BRIEFING_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p0-briefing:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cloud-infrastructure-p1-briefing-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P1_BRIEFING_MODEL: ${CLOUD_INFRASTRUCTURE_P1_BRIEFING_MODEL:-${P1_BRIEFING_MODEL:-gpt-5-nano}}
      P1_BRIEFING_BATCH_SIZE: ${CLOUD_INFRASTRUCTURE_P1_BRIEFING_BATCH_SIZE:-10}
      P1_BRIEFING_POLL_INTERVAL_SECONDS: ${CLOUD_INFRASTRUCTURE_P1_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P1_BRIEFING_WINDOW_HOURS: ${CLOUD_INFRASTRUCTURE_P1_BRIEFING_WINDOW_HOURS:-168}
      P1_BRIEFING_CATEGORY_SLUG: cloud-infrastructure
      P1_BRIEFING_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p1-briefing:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cloud-infrastructure-p2-briefing-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P2_BRIEFING_MODEL: ${CLOUD_INFRASTRUCTURE_P2_BRIEFING_MODEL:-${P2_BRIEFING_MODEL:-gpt-5-nano}}
      P2_BRIEFING_BATCH_SIZE: ${CLOUD_INFRASTRUCTURE_P2_BRIEFING_BATCH_SIZE:-20}
      P2_BRIEFING_POLL_INTERVAL_SECONDS: ${CLOUD_INFRASTRUCTURE_P2_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P2_BRIEFING_WINDOW_HOURS: ${CLOUD_INFRASTRUCTURE_P2_BRIEFING_WINDOW_HOURS:-168}
      P2_BRIEFING_CATEGORY_SLUG: cloud-infrastructure
      P2_BRIEFING_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p2-briefing:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cloud-infrastructure-p3-briefing-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P3_BRIEFING_MODEL: ${CLOUD_INFRASTRUCTURE_P3_BRIEFING_MODEL:-${P3_BRIEFING_MODEL:-gpt-5-nano}}
      P3_BRIEFING_BATCH_SIZE: ${CLOUD_INFRASTRUCTURE_P3_BRIEFING_BATCH_SIZE:-20}
      P3_BRIEFING_POLL_INTERVAL_SECONDS: ${CLOUD_INFRASTRUCTURE_P3_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P3_BRIEFING_WINDOW_HOURS: ${CLOUD_INFRASTRUCTURE_P3_BRIEFING_WINDOW_HOURS:-168}
      P3_BRIEFING_CATEGORY_SLUG: cloud-infrastructure
      P3_BRIEFING_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p3-briefing:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  semiconductors-cluster-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      CLUSTER_BATCH_SIZE: ${SEMICONDUCTORS_CLUSTER_BATCH_SIZE:-200}
      CLUSTER_POLL_INTERVAL_SECONDS: ${SEMICONDUCTORS_CLUSTER_POLL_INTERVAL_SECONDS:-300}
      CLUSTER_SIMILARITY_THRESHOLD: ${SEMICONDUCTORS_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CLUSTER_CATEGORY_SLUG: semiconductors
      CLUSTER_WINDOW_DAYS: ${SEMICONDUCTORS_CLUSTER_WINDOW_DAYS:-7}
      CLUSTER_RUN_UNTIL_EMPTY: "true"
    command: ["npm", "run", "cluster:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  semiconductors-impact-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-${MATTERMOST_IMAGE_OPENROUTER_API_KEY:-}}
      OPENROUTER_REFERER: ${OPENROUTER_REFERER:-${MATTERMOST_IMAGE_OPENROUTER_REFERER:-}}
      OPENROUTER_TITLE: ${OPENROUTER_TITLE:-SignalRSS}
      OPENROUTER_REQUEST_TIMEOUT_MS: ${OPENROUTER_REQUEST_TIMEOUT_MS:-45000}
      GROQ_API_KEY: ${GROQ_API_KEY:-}
      GROQ_BASE_URL: ${GROQ_BASE_URL:-https://api.groq.com/openai/v1}
      GROQ_REQUEST_TIMEOUT_MS: ${GROQ_REQUEST_TIMEOUT_MS:-45000}
      NVIDIA_API_KEY: ${NVIDIA_API_KEY:-${MATTERMOST_IMAGE_NVIDIA_API_KEY:-}}
      NVIDIA_BASE_URL: ${NVIDIA_BASE_URL:-https://integrate.api.nvidia.com}
      NVIDIA_REQUEST_TIMEOUT_MS: ${NVIDIA_REQUEST_TIMEOUT_MS:-45000}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      IMPACT_MODEL: ${SEMICONDUCTORS_IMPACT_MODEL:-${IMPACT_MODEL:-gpt-5-nano}}
      IMPACT_RUBRIC_VERSION: ${IMPACT_RUBRIC_VERSION:-impact-v2}
      IMPACT_CATEGORY_GUIDANCE_JSON: ${IMPACT_CATEGORY_GUIDANCE_JSON:-}
      IMPACT_MAX_OUTPUT_TOKENS: ${IMPACT_MAX_OUTPUT_TOKENS:-2200}
      IMPACT_MODEL_FALLBACKS: ${IMPACT_MODEL_FALLBACKS:-nvidia:openai/gpt-oss-120b,groq:openai/gpt-oss-120b,openrouter:openai/gpt-oss-120b:free,groq:llama-3.3-70b-versatile,groq:openai/gpt-oss-20b,openrouter:nvidia/nemotron-3-super-120b-a12b:free,openai:gpt-5-nano}
      IMPACT_BATCH_SIZE: ${SEMICONDUCTORS_IMPACT_BATCH_SIZE:-1}
      IMPACT_POLL_INTERVAL_SECONDS: ${SEMICONDUCTORS_IMPACT_POLL_INTERVAL_SECONDS:-300}
      IMPACT_WINDOW_HOURS: ${SEMICONDUCTORS_IMPACT_WINDOW_HOURS:-168}
      IMPACT_CATEGORY_SLUG: semiconductors
      IMPACT_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "impact:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  semiconductors-p0-adjudication-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P0_ADJUDICATION_MODEL: ${SEMICONDUCTORS_P0_ADJUDICATION_MODEL:-${P0_ADJUDICATION_MODEL:-gpt-5-nano}}
      P0_ADJUDICATION_BATCH_SIZE: ${SEMICONDUCTORS_P0_ADJUDICATION_BATCH_SIZE:-12}
      P0_ADJUDICATION_POLL_INTERVAL_SECONDS: ${SEMICONDUCTORS_P0_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P0_ADJUDICATION_WINDOW_HOURS: ${SEMICONDUCTORS_P0_ADJUDICATION_WINDOW_HOURS:-168}
      P0_ADJUDICATION_CATEGORY_SLUG: semiconductors
      P0_ADJUDICATION_MIN_CENTROID_SIMILARITY: ${SEMICONDUCTORS_P0_ADJUDICATION_MIN_CENTROID_SIMILARITY:-0.70}
      P0_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P0_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P0_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${SEMICONDUCTORS_P0_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.72}
      P0_ADJUDICATION_MERGE_CONFIDENCE: ${P0_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p0-adjudication:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  semiconductors-p1-adjudication-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P1_ADJUDICATION_MODEL: ${SEMICONDUCTORS_P1_ADJUDICATION_MODEL:-${P1_ADJUDICATION_MODEL:-gpt-5-nano}}
      P1_ADJUDICATION_BATCH_SIZE: ${SEMICONDUCTORS_P1_ADJUDICATION_BATCH_SIZE:-20}
      P1_ADJUDICATION_POLL_INTERVAL_SECONDS: ${SEMICONDUCTORS_P1_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P1_ADJUDICATION_WINDOW_HOURS: ${SEMICONDUCTORS_P1_ADJUDICATION_WINDOW_HOURS:-168}
      P1_ADJUDICATION_CATEGORY_SLUG: semiconductors
      P1_ADJUDICATION_P1P1_MIN_CENTROID_SIMILARITY: ${SEMICONDUCTORS_P1_ADJUDICATION_P1P1_MIN_CENTROID_SIMILARITY:-0.72}
      P1_ADJUDICATION_P1P0_MIN_CENTROID_SIMILARITY: ${SEMICONDUCTORS_P1_ADJUDICATION_P1P0_MIN_CENTROID_SIMILARITY:-0.70}
      P1_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P1_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P1_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${SEMICONDUCTORS_P1_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.74}
      P1_ADJUDICATION_MERGE_CONFIDENCE: ${P1_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p1-adjudication:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  semiconductors-p2-adjudication-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P2_ADJUDICATION_MODEL: ${SEMICONDUCTORS_P2_ADJUDICATION_MODEL:-${P2_ADJUDICATION_MODEL:-gpt-5-nano}}
      P2_ADJUDICATION_BATCH_SIZE: ${SEMICONDUCTORS_P2_ADJUDICATION_BATCH_SIZE:-20}
      P2_ADJUDICATION_POLL_INTERVAL_SECONDS: ${SEMICONDUCTORS_P2_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P2_ADJUDICATION_WINDOW_HOURS: ${SEMICONDUCTORS_P2_ADJUDICATION_WINDOW_HOURS:-168}
      P2_ADJUDICATION_CATEGORY_SLUG: semiconductors
      P2_ADJUDICATION_P2P2_MIN_CENTROID_SIMILARITY: ${SEMICONDUCTORS_P2_ADJUDICATION_P2P2_MIN_CENTROID_SIMILARITY:-0.72}
      P2_ADJUDICATION_P2P1_MIN_CENTROID_SIMILARITY: ${SEMICONDUCTORS_P2_ADJUDICATION_P2P1_MIN_CENTROID_SIMILARITY:-0.70}
      P2_ADJUDICATION_P2P0_MIN_CENTROID_SIMILARITY: ${SEMICONDUCTORS_P2_ADJUDICATION_P2P0_MIN_CENTROID_SIMILARITY:-0.70}
      P2_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P2_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P2_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${SEMICONDUCTORS_P2_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.72}
      P2_ADJUDICATION_MERGE_CONFIDENCE: ${P2_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p2-adjudication:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  semiconductors-p3-adjudication-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P3_ADJUDICATION_MODEL: ${SEMICONDUCTORS_P3_ADJUDICATION_MODEL:-${P3_ADJUDICATION_MODEL:-gpt-5-nano}}
      P3_ADJUDICATION_BATCH_SIZE: ${SEMICONDUCTORS_P3_ADJUDICATION_BATCH_SIZE:-20}
      P3_ADJUDICATION_POLL_INTERVAL_SECONDS: ${SEMICONDUCTORS_P3_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P3_ADJUDICATION_WINDOW_HOURS: ${SEMICONDUCTORS_P3_ADJUDICATION_WINDOW_HOURS:-168}
      P3_ADJUDICATION_CATEGORY_SLUG: semiconductors
      P3_ADJUDICATION_P3P3_MIN_CENTROID_SIMILARITY: ${SEMICONDUCTORS_P3_ADJUDICATION_P3P3_MIN_CENTROID_SIMILARITY:-0.72}
      P3_ADJUDICATION_P3P2_MIN_CENTROID_SIMILARITY: ${SEMICONDUCTORS_P3_ADJUDICATION_P3P2_MIN_CENTROID_SIMILARITY:-0.70}
      P3_ADJUDICATION_P3P1_MIN_CENTROID_SIMILARITY: ${SEMICONDUCTORS_P3_ADJUDICATION_P3P1_MIN_CENTROID_SIMILARITY:-0.70}
      P3_ADJUDICATION_P3P0_MIN_CENTROID_SIMILARITY: ${SEMICONDUCTORS_P3_ADJUDICATION_P3P0_MIN_CENTROID_SIMILARITY:-0.70}
      P3_ADJUDICATION_MAX_CENTROID_SIMILARITY: ${P3_ADJUDICATION_MAX_CENTROID_SIMILARITY:-0.82}
      P3_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${SEMICONDUCTORS_P3_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.72}
      P3_ADJUDICATION_MERGE_CONFIDENCE: ${P3_ADJUDICATION_MERGE_CONFIDENCE:-0.70}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p3-adjudication:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  semiconductors-p0-briefing-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P0_BRIEFING_MODEL: ${SEMICONDUCTORS_P0_BRIEFING_MODEL:-${P0_BRIEFING_MODEL:-gpt-5-nano}}
      P0_BRIEFING_BATCH_SIZE: ${SEMICONDUCTORS_P0_BRIEFING_BATCH_SIZE:-10}
      P0_BRIEFING_POLL_INTERVAL_SECONDS: ${SEMICONDUCTORS_P0_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P0_BRIEFING_WINDOW_HOURS: ${SEMICONDUCTORS_P0_BRIEFING_WINDOW_HOURS:-168}
      P0_BRIEFING_CATEGORY_SLUG: semiconductors
      P0_BRIEFING_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p0-briefing:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  semiconductors-p1-briefing-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P1_BRIEFING_MODEL: ${SEMICONDUCTORS_P1_BRIEFING_MODEL:-${P1_BRIEFING_MODEL:-gpt-5-nano}}
      P1_BRIEFING_BATCH_SIZE: ${SEMICONDUCTORS_P1_BRIEFING_BATCH_SIZE:-10}
      P1_BRIEFING_POLL_INTERVAL_SECONDS: ${SEMICONDUCTORS_P1_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P1_BRIEFING_WINDOW_HOURS: ${SEMICONDUCTORS_P1_BRIEFING_WINDOW_HOURS:-168}
      P1_BRIEFING_CATEGORY_SLUG: semiconductors
      P1_BRIEFING_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p1-briefing:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  semiconductors-p2-briefing-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P2_BRIEFING_MODEL: ${SEMICONDUCTORS_P2_BRIEFING_MODEL:-${P2_BRIEFING_MODEL:-gpt-5-nano}}
      P2_BRIEFING_BATCH_SIZE: ${SEMICONDUCTORS_P2_BRIEFING_BATCH_SIZE:-20}
      P2_BRIEFING_POLL_INTERVAL_SECONDS: ${SEMICONDUCTORS_P2_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P2_BRIEFING_WINDOW_HOURS: ${SEMICONDUCTORS_P2_BRIEFING_WINDOW_HOURS:-168}
      P2_BRIEFING_CATEGORY_SLUG: semiconductors
      P2_BRIEFING_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p2-briefing:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  semiconductors-p3-briefing-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      P3_BRIEFING_MODEL: ${SEMICONDUCTORS_P3_BRIEFING_MODEL:-${P3_BRIEFING_MODEL:-gpt-5-nano}}
      P3_BRIEFING_BATCH_SIZE: ${SEMICONDUCTORS_P3_BRIEFING_BATCH_SIZE:-20}
      P3_BRIEFING_POLL_INTERVAL_SECONDS: ${SEMICONDUCTORS_P3_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P3_BRIEFING_WINDOW_HOURS: ${SEMICONDUCTORS_P3_BRIEFING_WINDOW_HOURS:-168}
      P3_BRIEFING_CATEGORY_SLUG: semiconductors
      P3_BRIEFING_RUN_UNTIL_EMPTY: "true"
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "p3-briefing:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  category-cluster-worker:
    <<: *app-hardening
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      CLUSTER_BATCH_SIZE: ${CATEGORY_CLUSTER_BATCH_SIZE:-200}
      CLUSTER_POLL_INTERVAL_SECONDS: ${CATEGORY_CLUSTER_POLL_INTERVAL_SECONDS:-300}
      CLUSTER_SIMILARITY_THRESHOLD: ${CLUSTER_SIMILARITY_THRESHOLD:-0.65}
      CLUSTER_AI_SIMILARITY_THRESHOLD: ${CLUSTER_AI_SIMILARITY_THRESHOLD:-0.78}
      CYBERSECURITY_CLUSTER_SIMILARITY_THRESHOLD: ${CYBERSECURITY_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CLOUD_INFRASTRUCTURE_CLUSTER_SIMILARITY_THRESHOLD: ${CLOUD_INFRASTRUCTURE_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      AUTOMOTIVE_MOBILITY_CLUSTER_SIMILARITY_THRESHOLD: ${AUTOMOTIVE_MOBILITY_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      SEMICONDUCTORS_CLUSTER_SIMILARITY_THRESHOLD: ${SEMICONDUCTORS_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      SOFTWARE_DEVELOPMENT_CLUSTER_SIMILARITY_THRESHOLD: ${SOFTWARE_DEVELOPMENT_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CONSUMER_ELECTRONICS_CLUSTER_SIMILARITY_THRESHOLD: ${CONSUMER_ELECTRONICS_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      GAMING_CLUSTER_SIMILARITY_THRESHOLD: ${GAMING_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      FINTECH_CRYPTO_CLUSTER_SIMILARITY_THRESHOLD: ${FINTECH_CRYPTO_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CRYPTO_WEB3_CLUSTER_SIMILARITY_THRESHOLD: ${CRYPTO_WEB3_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      ENTERPRISE_TECHNOLOGY_CLUSTER_SIMILARITY_THRESHOLD: ${ENTERPRISE_TECHNOLOGY_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      STARTUPS_VENTURE_CAPITAL_CLUSTER_SIMILARITY_THRESHOLD: ${STARTUPS_VENTURE_CAPITAL_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      SCIENCE_RESEARCH_CLUSTER_SIMILARITY_THRESHOLD: ${SCIENCE_RESEARCH_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      POLICY_REGULATION_CLUSTER_SIMILARITY_THRESHOLD: ${POLICY_REGULATION_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CLUSTER_WINDOW_DAYS: ${CLUSTER_WINDOW_DAYS:-7}
      CATEGORY_WORKER_INCLUDE_SLUGS: ${CATEGORY_WORKER_INCLUDE_SLUGS:-}
      CATEGORY_WORKER_EXCLUDE_SLUGS: ${CATEGORY_WORKER_EXCLUDE_SLUGS:-}
    command: ["npm", "run", "category-cluster:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  category-impact-worker:
    <<: *category-impact-worker-base

  category-impact-worker-2:
    <<: *category-impact-worker-base

  category-impact-worker-3:
    <<: *category-impact-worker-base

  category-impact-worker-4:
    <<: *category-impact-worker-base

  category-impact-worker-5:
    <<: *category-impact-worker-base

  category-impact-worker-6:
    <<: *category-impact-worker-base

  category-adjudication-worker:
    <<: *app-hardening
    build: .
    environment:
      <<: [*db-env, *langfuse-env]
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      P0_ADJUDICATION_MODEL: ${P0_ADJUDICATION_MODEL:-gpt-5-nano}
      P1_ADJUDICATION_MODEL: ${P1_ADJUDICATION_MODEL:-gpt-5-nano}
      P2_ADJUDICATION_MODEL: ${P2_ADJUDICATION_MODEL:-gpt-5-nano}
      P3_ADJUDICATION_MODEL: ${P3_ADJUDICATION_MODEL:-gpt-5-nano}
      P0_ADJUDICATION_BATCH_SIZE: ${CATEGORY_P0_ADJUDICATION_BATCH_SIZE:-12}
      P1_ADJUDICATION_BATCH_SIZE: ${CATEGORY_P1_ADJUDICATION_BATCH_SIZE:-20}
      P2_ADJUDICATION_BATCH_SIZE: ${CATEGORY_P2_ADJUDICATION_BATCH_SIZE:-20}
      P3_ADJUDICATION_BATCH_SIZE: ${CATEGORY_P3_ADJUDICATION_BATCH_SIZE:-20}
      P0_ADJUDICATION_POLL_INTERVAL_SECONDS: ${CATEGORY_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P1_ADJUDICATION_POLL_INTERVAL_SECONDS: ${CATEGORY_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P2_ADJUDICATION_POLL_INTERVAL_SECONDS: ${CATEGORY_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P3_ADJUDICATION_POLL_INTERVAL_SECONDS: ${CATEGORY_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      P0_ADJUDICATION_WINDOW_HOURS: ${P0_ADJUDICATION_WINDOW_HOURS:-168}
      P1_ADJUDICATION_WINDOW_HOURS: ${P1_ADJUDICATION_WINDOW_HOURS:-168}
      P2_ADJUDICATION_WINDOW_HOURS: ${P2_ADJUDICATION_WINDOW_HOURS:-168}
      P3_ADJUDICATION_WINDOW_HOURS: ${P3_ADJUDICATION_WINDOW_HOURS:-168}
      PRIORITY_ADJUDICATION_VECTOR_NEIGHBORS: ${PRIORITY_ADJUDICATION_VECTOR_NEIGHBORS:-40}
      PRIORITY_ADJUDICATION_MIN_STRONG_TOKEN_OVERLAP: ${PRIORITY_ADJUDICATION_MIN_STRONG_TOKEN_OVERLAP:-2}
      CLOUD_INFRASTRUCTURE_P0_ADJUDICATION_MIN_CENTROID_SIMILARITY: ${CLOUD_INFRASTRUCTURE_P0_ADJUDICATION_MIN_CENTROID_SIMILARITY:-0.62}
      CONSUMER_ELECTRONICS_P1_ADJUDICATION_MERGE_CONFIDENCE: ${CONSUMER_ELECTRONICS_P1_ADJUDICATION_MERGE_CONFIDENCE:-0.60}
      CONSUMER_ELECTRONICS_P2_ADJUDICATION_MERGE_CONFIDENCE: ${CONSUMER_ELECTRONICS_P2_ADJUDICATION_MERGE_CONFIDENCE:-0.60}
      CONSUMER_ELECTRONICS_P3_ADJUDICATION_MERGE_CONFIDENCE: ${CONSUMER_ELECTRONICS_P3_ADJUDICATION_MERGE_CONFIDENCE:-0.60}
      CATEGORY_WORKER_INCLUDE_SLUGS: ${CATEGORY_WORKER_INCLUDE_SLUGS:-}
      CATEGORY_WORKER_EXCLUDE_SLUGS: ${CATEGORY_WORKER_EXCLUDE_SLUGS:-}
    command: ["npm", "run", "category-adjudication:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  category-briefing-worker:
    <<: *app-hardening
    build: .
    environment:
      <<: [*db-env, *llm-env, *langfuse-env]
      P0_BRIEFING_MODEL: ${P0_BRIEFING_MODEL:-gpt-5-nano}
      BRIEFING_MODEL_FALLBACKS: ${BRIEFING_MODEL_FALLBACKS:-mistral:mistral-small-latest,nvidia:openai/gpt-oss-120b,github:openai/gpt-4.1-mini,github:openai/gpt-4o-mini,github:mistral-ai/mistral-small-2503,github:meta/meta-llama-3.1-8b-instruct,groq:openai/gpt-oss-120b,groq:llama-3.3-70b-versatile,groq:openai/gpt-oss-20b,openrouter:openai/gpt-oss-120b:free,openrouter:nvidia/nemotron-3-super-120b-a12b:free,gemini:gemini-3-flash-preview,gemini:gemma-4-31b-it,sambanova:gpt-oss-120b,cloudflare:@cf/openai/gpt-oss-120b,cerebras:llama3.1-8b,llmrack:qwen-2.5-7b,openai:gpt-5-nano}
      BRIEFING_OPENAI_FALLBACK_LEVELS: ${BRIEFING_OPENAI_FALLBACK_LEVELS:-P0,P1,P2,P3}
      BRIEFING_OUTPUT_LANGUAGE: ${BRIEFING_OUTPUT_LANGUAGE:-Spanish}
      BRIEFING_VERSION_SUFFIX: ${BRIEFING_VERSION_SUFFIX:-}
      BRIEFING_CATEGORY_GUIDANCE_JSON: ${BRIEFING_CATEGORY_GUIDANCE_JSON:-}
      BRIEFING_MIN_PUBLISHED_AT: ${BRIEFING_MIN_PUBLISHED_AT:-}
      P1_BRIEFING_MODEL: ${P1_BRIEFING_MODEL:-gpt-5-nano}
      P2_BRIEFING_MODEL: ${P2_BRIEFING_MODEL:-gpt-5-nano}
      P3_BRIEFING_MODEL: ${P3_BRIEFING_MODEL:-gpt-5-nano}
      P0_BRIEFING_BATCH_SIZE: ${CATEGORY_P0_BRIEFING_BATCH_SIZE:-10}
      P1_BRIEFING_BATCH_SIZE: ${CATEGORY_P1_BRIEFING_BATCH_SIZE:-10}
      P2_BRIEFING_BATCH_SIZE: ${CATEGORY_P2_BRIEFING_BATCH_SIZE:-20}
      P3_BRIEFING_BATCH_SIZE: ${CATEGORY_P3_BRIEFING_BATCH_SIZE:-20}
      CATEGORY_BRIEFING_BATCHES_PER_TICK: ${CATEGORY_BRIEFING_BATCHES_PER_TICK:-3}
      CATEGORY_BRIEFING_CONCURRENCY: ${CATEGORY_BRIEFING_CONCURRENCY:-1}
      CATEGORY_BRIEFING_FREE_BATCH_SIZE: ${CATEGORY_BRIEFING_FREE_BATCH_SIZE:-5}
      CATEGORY_BRIEFING_OPENAI_BATCH_SIZE: ${CATEGORY_BRIEFING_OPENAI_BATCH_SIZE:-10}
      CATEGORY_BRIEFING_EXCLUDE_LEVELS: ${CATEGORY_BRIEFING_EXCLUDE_LEVELS:-}
      P0_BRIEFING_POLL_INTERVAL_SECONDS: ${CATEGORY_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P1_BRIEFING_POLL_INTERVAL_SECONDS: ${CATEGORY_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P2_BRIEFING_POLL_INTERVAL_SECONDS: ${CATEGORY_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      P3_BRIEFING_POLL_INTERVAL_SECONDS: ${CATEGORY_BRIEFING_POLL_INTERVAL_SECONDS:-300}
      BRIEFING_CLAIM_STALE_MINUTES: ${BRIEFING_CLAIM_STALE_MINUTES:-15}
      BRIEFING_STARTUP_CLAIM_CLEANUP_MINUTES: ${BRIEFING_STARTUP_CLAIM_CLEANUP_MINUTES:-2}
      P0_BRIEFING_WINDOW_HOURS: ${P0_BRIEFING_WINDOW_HOURS:-168}
      P1_BRIEFING_WINDOW_HOURS: ${P1_BRIEFING_WINDOW_HOURS:-168}
      P2_BRIEFING_WINDOW_HOURS: ${P2_BRIEFING_WINDOW_HOURS:-168}
      P3_BRIEFING_WINDOW_HOURS: ${P3_BRIEFING_WINDOW_HOURS:-168}
      CATEGORY_WORKER_INCLUDE_SLUGS: ${CATEGORY_WORKER_INCLUDE_SLUGS:-}
      CATEGORY_WORKER_EXCLUDE_SLUGS: ${CATEGORY_WORKER_EXCLUDE_SLUGS:-}
    command: ["npm", "run", "category-briefing:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cross-category-adjudication-worker:
    <<: *app-hardening
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      CROSS_CATEGORY_ADJUDICATION_MODEL: ${CROSS_CATEGORY_ADJUDICATION_MODEL:-gpt-5-nano}
      CROSS_CATEGORY_ADJUDICATION_BATCH_SIZE: ${CROSS_CATEGORY_ADJUDICATION_BATCH_SIZE:-8}
      CROSS_CATEGORY_ADJUDICATION_POLL_INTERVAL_SECONDS: ${CROSS_CATEGORY_ADJUDICATION_POLL_INTERVAL_SECONDS:-300}
      CROSS_CATEGORY_ADJUDICATION_WINDOW_HOURS: ${CROSS_CATEGORY_ADJUDICATION_WINDOW_HOURS:-168}
      CROSS_CATEGORY_ADJUDICATION_SEED_WINDOW_HOURS: ${CROSS_CATEGORY_ADJUDICATION_SEED_WINDOW_HOURS:-1}
      CROSS_CATEGORY_ADJUDICATION_CATEGORIES: ${CROSS_CATEGORY_ADJUDICATION_CATEGORIES:-artificial-intelligence,cybersecurity,cloud-infrastructure,semiconductors,software-development,consumer-electronics,gaming,enterprise-technology,startups-venture-capital,policy-regulation,science-research,automotive-mobility,fintech-crypto,crypto-web3}
      CROSS_CATEGORY_ADJUDICATION_LEVELS: ${CROSS_CATEGORY_ADJUDICATION_LEVELS:-P0,P1,P2,P3}
      CROSS_CATEGORY_ADJUDICATION_MIN_CENTROID_SIMILARITY: ${CROSS_CATEGORY_ADJUDICATION_MIN_CENTROID_SIMILARITY:-0.76}
      CROSS_CATEGORY_ADJUDICATION_ARTICLE_SCAN_MIN_CENTROID_SIMILARITY: ${CROSS_CATEGORY_ADJUDICATION_ARTICLE_SCAN_MIN_CENTROID_SIMILARITY:-0.68}
      CROSS_CATEGORY_ADJUDICATION_MIN_ARTICLE_SIMILARITY: ${CROSS_CATEGORY_ADJUDICATION_MIN_ARTICLE_SIMILARITY:-0.80}
      CROSS_CATEGORY_ADJUDICATION_MIN_TOKEN_OVERLAP: ${CROSS_CATEGORY_ADJUDICATION_MIN_TOKEN_OVERLAP:-2}
      CROSS_CATEGORY_ADJUDICATION_MAX_TOKEN_FANOUT: ${CROSS_CATEGORY_ADJUDICATION_MAX_TOKEN_FANOUT:-120}
      CROSS_CATEGORY_ADJUDICATION_MERGE_CONFIDENCE: ${CROSS_CATEGORY_ADJUDICATION_MERGE_CONFIDENCE:-0.78}
      CROSS_CATEGORY_ADJUDICATION_APPLY_MERGES: ${CROSS_CATEGORY_ADJUDICATION_APPLY_MERGES:-true}
      CROSS_CATEGORY_ADJUDICATION_RUN_UNTIL_EMPTY: ${CROSS_CATEGORY_ADJUDICATION_RUN_UNTIL_EMPTY:-true}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-https://cloud.langfuse.com}
    command: ["npm", "run", "cross-category-adjudication:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  cluster-worker:
    build: .
    profiles:
      - legacy-category-workers
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      CLUSTER_BATCH_SIZE: ${CLUSTER_BATCH_SIZE:-200}
      CLUSTER_POLL_INTERVAL_SECONDS: ${CLUSTER_POLL_INTERVAL_SECONDS:-300}
      CLUSTER_SIMILARITY_THRESHOLD: ${CLUSTER_SIMILARITY_THRESHOLD:-0.65}
      CLUSTER_AI_SIMILARITY_THRESHOLD: ${CLUSTER_AI_SIMILARITY_THRESHOLD:-0.78}
      CYBERSECURITY_CLUSTER_SIMILARITY_THRESHOLD: ${CYBERSECURITY_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CLOUD_INFRASTRUCTURE_CLUSTER_SIMILARITY_THRESHOLD: ${CLOUD_INFRASTRUCTURE_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      AUTOMOTIVE_MOBILITY_CLUSTER_SIMILARITY_THRESHOLD: ${AUTOMOTIVE_MOBILITY_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      SEMICONDUCTORS_CLUSTER_SIMILARITY_THRESHOLD: ${SEMICONDUCTORS_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      SOFTWARE_DEVELOPMENT_CLUSTER_SIMILARITY_THRESHOLD: ${SOFTWARE_DEVELOPMENT_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CONSUMER_ELECTRONICS_CLUSTER_SIMILARITY_THRESHOLD: ${CONSUMER_ELECTRONICS_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      GAMING_CLUSTER_SIMILARITY_THRESHOLD: ${GAMING_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      FINTECH_CRYPTO_CLUSTER_SIMILARITY_THRESHOLD: ${FINTECH_CRYPTO_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CRYPTO_WEB3_CLUSTER_SIMILARITY_THRESHOLD: ${CRYPTO_WEB3_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      ENTERPRISE_TECHNOLOGY_CLUSTER_SIMILARITY_THRESHOLD: ${ENTERPRISE_TECHNOLOGY_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      STARTUPS_VENTURE_CAPITAL_CLUSTER_SIMILARITY_THRESHOLD: ${STARTUPS_VENTURE_CAPITAL_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      SCIENCE_RESEARCH_CLUSTER_SIMILARITY_THRESHOLD: ${SCIENCE_RESEARCH_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      POLICY_REGULATION_CLUSTER_SIMILARITY_THRESHOLD: ${POLICY_REGULATION_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CLUSTER_CATEGORY_SLUG: ${CLUSTER_CATEGORY_SLUG:-}
      CLUSTER_WINDOW_DAYS: ${CLUSTER_WINDOW_DAYS:-7}
      CLUSTER_RUN_UNTIL_EMPTY: "true"
    command: ["npm", "run", "cluster:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

  api:
    <<: *app-hardening
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      API_PORT: 3000
      API_REQUEST_TIMEOUT_MS: ${API_REQUEST_TIMEOUT_MS:-30000}
      API_HEADERS_TIMEOUT_MS: ${API_HEADERS_TIMEOUT_MS:-10000}
      API_KEEP_ALIVE_TIMEOUT_MS: ${API_KEEP_ALIVE_TIMEOUT_MS:-5000}
      API_MAX_URL_LENGTH: ${API_MAX_URL_LENGTH:-2048}
      API_WRITE_RATE_LIMIT_WINDOW_MS: ${API_WRITE_RATE_LIMIT_WINDOW_MS:-60000}
      API_WRITE_RATE_LIMIT_MAX: ${API_WRITE_RATE_LIMIT_MAX:-120}
      RSS_PUBLIC_BASE_URL: ${RSS_PUBLIC_BASE_URL:-http://localhost:3000}
      MATTERMOST_CATEGORY_SLUGS: ${MATTERMOST_CATEGORY_SLUGS:-artificial-intelligence}
      MATTERMOST_CHANNEL: ${MATTERMOST_CHANNEL:-}
      MATTERMOST_CHANNELS_BY_CATEGORY: ${MATTERMOST_CHANNELS_BY_CATEGORY:-}
      MATTERMOST_GENERATED_THUMBNAIL_DIR: ${MATTERMOST_GENERATED_THUMBNAIL_DIR:-/app/data/generated-thumbnails}
      MATTERMOST_GENERATED_THUMBNAIL_PUBLIC_PATH: ${MATTERMOST_GENERATED_THUMBNAIL_PUBLIC_PATH:-/generated-thumbnails}
      IMPACT_MIN_PUBLISHED_AT: ${IMPACT_MIN_PUBLISHED_AT:-}
      BRIEFING_MIN_PUBLISHED_AT: ${BRIEFING_MIN_PUBLISHED_AT:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-small}
      CLUSTER_BATCH_SIZE: ${CLUSTER_BATCH_SIZE:-200}
      CLUSTER_SIMILARITY_THRESHOLD: ${CLUSTER_SIMILARITY_THRESHOLD:-0.65}
      CLUSTER_AI_SIMILARITY_THRESHOLD: ${CLUSTER_AI_SIMILARITY_THRESHOLD:-0.78}
      CYBERSECURITY_CLUSTER_SIMILARITY_THRESHOLD: ${CYBERSECURITY_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CLOUD_INFRASTRUCTURE_CLUSTER_SIMILARITY_THRESHOLD: ${CLOUD_INFRASTRUCTURE_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      AUTOMOTIVE_MOBILITY_CLUSTER_SIMILARITY_THRESHOLD: ${AUTOMOTIVE_MOBILITY_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      SEMICONDUCTORS_CLUSTER_SIMILARITY_THRESHOLD: ${SEMICONDUCTORS_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      SOFTWARE_DEVELOPMENT_CLUSTER_SIMILARITY_THRESHOLD: ${SOFTWARE_DEVELOPMENT_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CONSUMER_ELECTRONICS_CLUSTER_SIMILARITY_THRESHOLD: ${CONSUMER_ELECTRONICS_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      GAMING_CLUSTER_SIMILARITY_THRESHOLD: ${GAMING_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      FINTECH_CRYPTO_CLUSTER_SIMILARITY_THRESHOLD: ${FINTECH_CRYPTO_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CRYPTO_WEB3_CLUSTER_SIMILARITY_THRESHOLD: ${CRYPTO_WEB3_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      ENTERPRISE_TECHNOLOGY_CLUSTER_SIMILARITY_THRESHOLD: ${ENTERPRISE_TECHNOLOGY_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      STARTUPS_VENTURE_CAPITAL_CLUSTER_SIMILARITY_THRESHOLD: ${STARTUPS_VENTURE_CAPITAL_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      SCIENCE_RESEARCH_CLUSTER_SIMILARITY_THRESHOLD: ${SCIENCE_RESEARCH_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      POLICY_REGULATION_CLUSTER_SIMILARITY_THRESHOLD: ${POLICY_REGULATION_CLUSTER_SIMILARITY_THRESHOLD:-0.78}
      CLUSTER_WINDOW_DAYS: ${CLUSTER_WINDOW_DAYS:-7}
      CATEGORY_BRIEFING_EXCLUDE_LEVELS: ${CATEGORY_BRIEFING_EXCLUDE_LEVELS:-}
    command: ["npm", "run", "api"]
    ports:
      - "${API_BIND_ADDRESS:-127.0.0.1}:${API_PORT:-3000}:3000"
    volumes:
      - ./data:/app/data
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      seeder:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully

  worker:
    <<: *app-hardening
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgres://signalrss:signalrss@postgres:5432/signalrss}
      WORKER_POLL_INTERVAL_SECONDS: ${WORKER_POLL_INTERVAL_SECONDS:-300}
      WORKER_BATCH_SIZE: ${WORKER_BATCH_SIZE:-50}
      INGEST_WINDOW_DAYS: ${INGEST_WINDOW_DAYS:-7}
      FEED_MAX_BYTES: ${FEED_MAX_BYTES:-10485760}
    command: ["npm", "run", "worker"]
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      seeder:
        condition: service_completed_successfully
      category-seeder:
        condition: service_completed_successfully
    restart: unless-stopped

volumes:
  postgres18_data:
